# 系统基础 DAO 组件（nop-sys-dao）

## 1. 摘要（TL;DR）
1. `nop-sys-dao` 的 `app.orm.xml` 定义了序列、字典、多语言、审批、编码规则、事件队列、服务实例、变更日志等核心系统表，是平台级功能的实体基线。【F:nop-sys/nop-sys-dao/src/main/resources/_vfs/nop/sys/orm/_app.orm.xml†L1-L622】
2. `SysSequenceGenerator` 结合数据库配置、Snowflake、UUID 三种策略提供统一序列服务，内部以 `SeqItem` 缓存 `cacheSize/stepSize/nextValue`，不足时进入事务 `syncFromDb` 锁定 `NopSysSequence` 并批量前移 `nextValue`，缺失序列会按需回退默认或 UUID 占位；`SysCodeRuleGenerator` 读取序列结果拼装业务编码并在缺项时抛错阻断生成。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L44-L269】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/coderule/SysCodeRuleGenerator.java†L52-L69】
3. `SysDictLoader`/`SysI18nMessageLoader` 在 IoC 初始化阶段注册系统字典与多语言消息，实现运行期动态读取，并在租户未注入时跳过严格校验以保证启动成功。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/dict/SysDictLoader.java†L35-L91】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/i18n/SysI18nMessageLoader.java†L20-L35】
4. `OrmEntityChangeLogInterceptor` 在未显式关闭 `nop.orm.audit.enabled` 时识别带 `audit`/`audit-save` 标签的实体，在保存、更新、删除时写入 `NopSysChangeLog`，自动跳过版本与 `no-audit` 列；同时 `@BizMakerChecker` 标注的增删改动作会在 GraphQL 层注入 `MakerCheckerTryServiceAction`，默认 `nop.graphql.maker-checker.enabled=false` 需显式打开后才会改为审批流程，`MakerCheckerTryServiceAction` 会在 `CompletionStage` 完成后调用 `ApiResponse.buildSuccess` 返回审批编号并将请求落地到系统审批表。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/log/OrmEntityChangeLogInterceptor.java†L13-L94】【F:nop-sys/nop-sys-dao/src/main/resources/_vfs/nop/sys/beans/app-dao.beans.xml†L31-L36】【F:nop-graphql/nop-graphql-core/src/main/java/io/nop/graphql/core/GraphQLConfigs.java†L33-L44】【F:nop-biz/src/main/java/io/nop/biz/makerchecker/MakerCheckerTryServiceAction.java†L20-L66】【F:nop-biz/src/main/java/io/nop/biz/makerchecker/SendForCheckRequest.java†L17-L66】
5. `SysDaoNamingService` 把服务实例注册到 `NopSysServiceInstance` 表，续约时使用数据库估算时钟刷新 `updateTime` 并在查询服务列表时依赖相同阈值，但 `getInstances` 与 `cleanup` 仍使用本地 `System.currentTimeMillis()` 计算过滤/删除条件，因此需要保证节点时钟与数据库保持同步以免提前删除或长时间保留实例；metadata JSON 列最大 1000 字符、标签字符串列仅 100 字符且实例默认 `ephemeral=true`，需控制注册数据长度并按心跳周期续约以避免被清理；定时清理长时间未续约的临时节点，为基于数据库的注册中心提供落地实现。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/naming/SysDaoNamingService.java†L44-L163】【F:nop-sys/nop-sys-dao/src/main/resources/_vfs/nop/sys/orm/_app.orm.xml†L553-L589】【F:nop-cluster/nop-cluster-core/src/main/java/io/nop/cluster/discovery/ServiceInstance.java†L31-L110】
6. `SysDaoResourceLockManager` 利用 `nop_sys_lock` 表实现数据库分布式锁，默认 1s 等待、10s 租期，续约通过 `tryResetLease` 自增版本并依赖数据库估算时钟，`saveNew` 将 `AppConfig.appName()` 与 `IServerAddrFinder`（默认回退 `NetHelper.findLocalIp()`）写入锁记录便于排查持有者，`isHoldingLock` 重新读取锁行确认持有者与版本，`releaseLock` 直接删除实体避免命中旧缓存；`forceUnlock` 走 `deleteById` 不做版本校验，仅适合人工兜底，需要确认锁已放弃后再调用。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/SysDaoResourceLockManager.java†L39-L233】
7. `SysDaoLeaderElector` 继承轮询选主基类，以 `nop_sys_cluster_leader` 表记录租期和 Epoch，遇到超时或 Epoch 不一致会重新发起选举。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/elector/SysDaoLeaderElector.java†L24-L238】
8. `SysDaoMessageService` 轮询 `NopSysEvent` 表调度广播与非广播消息，默认 `RetryPolicy` 仅允许 2 次重试且至少延迟 10 秒，并通过 `LocalMessageService` 将数据库事件桥接到内存订阅与 `ack-` 前缀响应主题，需要 `<property>` 注入自定义策略与 `minProcessDelay` 才能缩短重试窗口。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L45-L312】【F:nop-message/nop-message-core/src/main/java/io/nop/message/core/local/LocalMessageService.java†L123-L200】【F:nop-commons/src/main/java/io/nop/commons/util/retry/RetryPolicy.java†L17-L180】【F:nop-api-core/src/main/java/io/nop/api/core/message/IMessageSubscriber.java†L10-L24】

## 2. 术语与边界（中+英）
| 术语 | 英文 | 边界说明 |
| --- | --- | --- |
| 系统序列 | System Sequence | `NopSysSequence` 保存序列名、步长、缓存等参数，供 `SysSequenceGenerator` 装载和自增，不负责业务规则拼装。【F:nop-sys/nop-sys-dao/src/main/resources/_vfs/nop/sys/orm/_app.orm.xml†L44-L83】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L44-L195】|
| 编码规则 | Code Rule | `NopSysCodeRule` 配置编码模式与引用的序列名，`SysCodeRuleGenerator` 负责读取规则并调用序列生成数字部分。【F:nop-sys/nop-sys-dao/src/main/resources/_vfs/nop/sys/orm/_app.orm.xml†L268-L311】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/coderule/SysCodeRuleGenerator.java†L52-L68】|
| 系统字典 | System Dictionary | 以 `NopSysDict`/`NopSysDictOption` 表为存储，通过 `SysDictLoader` 在 `DictProvider` 中注册，前缀限定在 `sys/` 命名空间。【F:nop-sys/nop-sys-dao/src/main/resources/_vfs/nop/sys/orm/_app.orm.xml†L84-L176】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/dict/SysDictLoader.java†L35-L91】|
| 系统事件 | System Event | `NopSysEvent` 表记录消息主题、负载、调度时间与重试次数，`SysDaoMessageService` 轮询此表触发监听器回调。【F:nop-sys/nop-sys-dao/src/main/resources/_vfs/nop/sys/orm/_app.orm.xml†L501-L552】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L137-L312】|
| 服务实例 | Service Instance | `NopSysServiceInstance` 表存储数据库注册中心实例信息，`SysDaoNamingService` 提供注册、续约与查询能力。【F:nop-sys/nop-sys-dao/src/main/resources/_vfs/nop/sys/orm/_app.orm.xml†L553-L590】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/naming/SysDaoNamingService.java†L27-L163】|
| 审批记录 | MakerChecker Record | `NopSysCheckerRecord`/`NopSysMakerCheckerRecord` 表持久化 MakerChecker 审批请求、try 结果与回调状态，供审批流程与业务补偿读取。【F:nop-sys/nop-sys-dao/src/main/resources/_vfs/nop/sys/orm/_app.orm.xml†L206-L276】|
| 变更日志 | Change Log | `NopSysChangeLog` 记录实体名称、主键、业务键、操作人、审批人、旧值/新值与时间戳等字段，为审计追溯提供结构化行数据。【F:nop-sys/nop-sys-meta/src/main/resources/_vfs/nop/sys/model/NopSysChangeLog/_NopSysChangeLog.xmeta†L1-L59】|
| 资源锁 | Resource Lock | `NopSysLock` 的复合主键由锁组+锁名组成，`SysDaoResourceLockManager` 以数据库行表示持有者与租期，实现跨节点互斥。【F:nop-sys/nop-sys-dao/src/main/resources/_vfs/nop/sys/orm/_app.orm.xml†L360-L439】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/SysDaoResourceLockManager.java†L39-L233】|

## 3. 事实（Facts）
- `app.orm.xml` 为 `nop-sys` 模块设定 Maven 坐标、默认方言、领域字典和十余个实体，包括序列、字典、编码规则、变量、通知模板、扩展字段、分布式锁、选主表、事件队列、服务实例、变更日志与标签等基础能力，为系统级服务提供统一 schema。【F:nop-sys/nop-sys-dao/src/main/resources/_vfs/nop/sys/orm/_app.orm.xml†L1-L622】
- `app-dao.beans.xml` 将系统级 Bean 注入 IoC：序列生成器启用 `lazyInit` 自动补齐默认序列，编码规则生成器声明为默认实现，资源锁管理器、选主器、消息服务根据配置开关注册；同时暴露 `SysDictLoader`、`SysI18nMessageLoader` 等加载器供平台启动时执行。【F:nop-sys/nop-sys-dao/src/main/resources/_vfs/nop/sys/beans/app-dao.beans.xml†L2-L38】
- `SysSequenceGenerator` 通过 `@InjectValue` 读取雪花算法 workerId 和默认起始值；若 `nop.sys.init-default-sequence` 为真，会在租户 0 上检查序列表并插入默认记录，生成时优先使用缓存批次，必要时回源数据库并锁表更新下一跳。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L44-L239】
- `addDefaultSequence` 在租户 `0` 环境下通过 `ContextProvider.runWithTenant` + `runLocal` 插入缺省记录，默认设置 `cacheSize=100`、`stepSize=1`、`seqType="seq"`、`seqName="default"` 并捕获并发插入导致的重复键异常，确保默认序列具备 100 个批量缓存且在多实例启动时不会重复写入。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L138-L175】
- `nop.sys.seq.default-seq-init-next-value` 决定默认序列 `default` 的初始 `nextValue`，`lazyInit` 在发现序列表为空时会带着该起始值插入基线记录，便于首次生成就对齐期望编号。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L104-L164】
- 当未显式配置 `nop.sys.seq.snowflake-worker-id` 时，`afterPropertiesSet` 会先读取 `CFG_HOST_ID`，若为空则回退 `NetHelper.findLocalIp()`，再使用 `HashHelper.murmur3_32` 对标识取模 1024 生成稳定 workerId，保证 Snowflake 序列在同一 hostId 下保持一致。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L126-L135】
- `SnowflakeSequenceGeneator` 在同一毫秒内使用 `MathHelper.secureRandom()` 初始化 0–99 的起始序列值，若同毫秒内 sequence 溢出会再次随机并自旋到下一毫秒，确保突发请求分布在多个序列槽位并降低碰撞概率。【F:nop-dao/src/main/java/io/nop/dao/seq/SnowflakeSequenceGeneator.java†L45-L109】
- `MathHelper.secureRandom()` 懒加载 `DefaultSecureRandom`（包装 `java.security.SecureRandom`），所有雪花随机数都复用单例安全随机器，必要时可通过 `MathHelper.registerSecureRandomImpl` 注入可控实现。【F:nop-commons/src/main/java/io/nop/commons/util/MathHelper.java†L108-L152】【F:nop-commons/src/main/java/io/nop/commons/util/random/DefaultSecureRandom.java†L12-L63】
- `runLocal` 统一通过 `ormTemplate.runInNewSession` + `transactionTemplate.runInTransaction(..., REQUIRES_NEW, …)` 在全新 Session 和事务中刷新序列，避免调用方所在事务的缓存影响 `nextValue` 读取或更新，保证批量前移操作具有独立提交边界。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L206-L239】
- 序列生成器在 JVM 内维护两个缓存：`cache` 用于已存在的序列，`defaultCache` 以 500 条、60 秒 TTL 缓存缺失序列的占位项，并提供 `clearCache`/`removeCache` 方法便于在修改数据库配置后主动失效本地缓存；但公开方法仅清理 `cache`，`defaultCache` 需要等待 TTL 或重启应用才能失效，占位项会在此期间继续返回随机值。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L52-L214】
- `generateLong`/`generateString` 在命中缓存时按 `cacheSize` 与 `stepSize` 递增 `nextValue`，缓存耗尽后通过 `syncFromDb` 在新事务中锁定 `NopSysSequence`、更新 `nextValue` 并把首个值返回；`findSeqItem` 先查询本地缓存，再尝试 `defaultCache` 或数据库，缺失序列且 `useDefault=true` 时回退 `default` 序列，`useDefault=false` 则创建 `useUuid=true` 的占位项并缓存，后续直接返回随机值。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L178-L269】
- `syncFromDb` 会在 `session.lock` 之后调用 `SeqItem.update(seq)` 校正配置，把小于等于 0 的步长强制重置为 1，并重新记录数据库中的 `cacheSize`；写回 `nextValue` 时按照 `当前 nextValue + cacheSize*stepSize`（若 `cacheSize=0` 则仅加一步长）计算批量窗口，确保返回的首个值之后的一整批编号已经被预留，即便调用方外层事务回滚也不会回退。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L62-L204】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L178-L206】
- `NopSysDaoConfigs` 暴露 `CFG_SYS_INIT_DEFAULT_SEQUENCE` 开关控制是否自动插入默认序列记录，默认开启。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/NopSysDaoConfigs.java†L15-L18】
- `SysCodeRuleGenerator` 根据规则名加载 `NopSysCodeRule`，校验序列名后以 `ICodeRule` 模板拼接当前时间与序列值；缺失规则或序列名时分别抛出 `ERR_SYS_UNKNOWN_CODE_RULE` 与 `ERR_SYS_CODE_RULE_EMPTY_SEQ_NAME`。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/coderule/SysCodeRuleGenerator.java†L52-L69】
- `SysDictLoader` 在 `PostConstruct` 时向 `DictProvider` 注册 `sys/` 前缀字典，`loadDict` 通过 `QueryBean` 联合 `NopSysDict`、`NopSysDictOption` 排序加载选项，并据 `isDeprecated` / `isInternal` 标记设定选项属性；若实体启用了租户且上下文缺少租户 ID，会跳过存在性检查以避免启动期失败。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/dict/SysDictLoader.java†L35-L91】
- `NopSysChangeLog` 元数据定义了主键、业务对象、对象 ID、业务键、操作名、属性名、旧值/新值、时间戳、应用、操作人、审批人等字段，值长度均限制在 `String` 100/4000 或 `Timestamp` 类型，确保审计行能够完整描述字段级变化。【F:nop-sys/nop-sys-meta/src/main/resources/_vfs/nop/sys/model/NopSysChangeLog/_NopSysChangeLog.xmeta†L1-L59】
- `app-dao.beans.xml` 将 `nopOrmEntityChangeLogInterceptor` 注册到 IoC，并以 `feature:on="nop.orm.audit.enabled == '' or nop.orm.audit.enabled"` 控制启用条件，默认在未配置时生效，显式设置 `nop.orm.audit.enabled=false` 可整体禁用实体审计入库。【F:nop-sys/nop-sys-dao/src/main/resources/_vfs/nop/sys/beans/app-dao.beans.xml†L31-L36】
- `_app.orm.xml` 通过 `orm:audit`、`orm:audit-save`、`orm:no-audit` 标签控制变更日志粒度：`audit` 同时记录保存/更新/删除，`audit-save` 仅覆盖保存分支，而列级的 `no-audit` 可显式排除密码等敏感字段；审计实体自身（`NopSysChangeLog`）默认跳过，避免递归写入。【F:nop-sys/nop-sys-dao/src/main/resources/_vfs/nop/sys/orm/_app.orm.xml†L1-L622】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/log/OrmEntityChangeLogInterceptor.java†L18-L94】
- `SysI18nMessageLoader` 在启动时一次性读取 `NopSysI18n` 表，将同一语言的键值聚合后注册到 `I18nMessageManager`，让运行期可以直接通过内存缓存提供翻译文本。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/i18n/SysI18nMessageLoader.java†L20-L35】
- `NopSysServiceInstance` 模型记录服务实例 ID、服务名、集群/分组、标签、地址端口、权重、健康与启用状态、是否临时、版本号与时间戳，metadata 以 JSON 字符串落地；`SysDaoNamingService.copyToEntity`/`fromEntity` 在注册与查询时负责序列化 metadata、标签并同步布尔字段，确保注册中心字段与运行态 `ServiceInstance` 一致。【F:nop-sys/nop-sys-dao/src/main/resources/_vfs/nop/sys/orm/_app.orm.xml†L554-L589】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/naming/SysDaoNamingService.java†L103-L163】
- `SysDaoNamingService` 使用 `IDaoProvider` 操作 `NopSysServiceInstance`，`registerInstance`/`updateInstance` 会刷新已有记录的 metadata、标签和 `updateTime`，否则插入新行；`getServices`/`getInstances` 会按最近更新时间过滤有效节点，`cleanup` 仅在配置了 `cleanupInterval` 时执行并删除超出心跳窗口且标记为临时的实例。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/naming/SysDaoNamingService.java†L44-L163】
- `ResourceLock` 以 `SysDaoResourceLockManager` 为后端封装默认 1s 等待、10s 租期的数据库锁实例；`tryLockWithLease` 首次写入失败后会基于 `IEstimatedClock` 循环执行 `saveNew`/`runInNewSession`，在删除旧记录前每 100ms 休眠一次，并在等待耗尽时返回 `null`，成功时把持有者地址、锁原因与版本封装进 `EntityResourceLockState` 供后续续约或释放使用；`getLock` 在未显式指定等待/租期时也会使用 Bean 属性 `defaultWaitTime/defaultLeaseTime` 创建 `ResourceLock` 对象，可通过 `<property>` 覆盖默认窗口以适配不同业务时长。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/SysDaoResourceLockManager.java†L38-L123】【F:nop-commons/src/main/java/io/nop/commons/concurrent/lock/impl/ResourceLock.java†L22-L86】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/EntityResourceLockState.java†L7-L44】
- `SysEventHelper.toSysEvent` 在入库时统一写入 `eventStatus=0`、`eventTime`、`processTime`、`bizDate` 与广播标记，并将消息类名或 `svcAction` 映射到 `eventName`；当消息是 `ApiRequest` 时会复制 headers、selection、data、bizKey/bizObjName，并基于 `bizObjName|bizKey` 计算分区哈希，而 `sendAsync`/`sendMultiAsync` 对非 `ApiRequest` 消息会使用随机 `partitionIndex` 与当前数据库时钟生成事件行。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysEventHelper.java†L19-L69】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L266-L304】
- `SysDaoMessageService.sendAsync/sendMultiAsync` 通过 `dao.getDbEstimatedClock()` 写入 `eventTime`/`processTime`，批量发送时缓存一次 `currentTime` 让同批消息共享时间戳，并为非 `ApiRequest` 负载生成 `MathHelper.random()` 的分区索引，保证事件排序依赖数据库时间而非节点本地时钟。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L266-L304】
- `forceUnlock` 直接调用 `orm().deleteById` 删除锁记录，不带版本匹配或持有者校验，适合人工兜底而非日常释放流程，若误用会强制删除仍在持有的锁行。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/SysDaoResourceLockManager.java†L90-L99】
- `saveNew` 会把 `AppConfig.appName()` 写入 `appId`，并通过注入的 `IServerAddrFinder`（缺省回退 `NetHelper.findLocalIp()`）填充 `holderAdder`，确保锁记录能追踪持有者节点的应用名与访问地址，用于调试和多网卡环境定位。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/SysDaoResourceLockManager.java†L179-L191】
- `registerInstance` 获取 `dao.getDbEstimatedClock()` 的 `maxCurrentTime` 写入 `updateTime`，即便应用节点时钟存在偏差也以数据库时间为准；同时 `getServices` 会使用 `clock.getMinCurrentTimeMillis()` 构造阈值，保证跨节点过滤逻辑依赖统一时间源。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/naming/SysDaoNamingService.java†L72-L110】
- `cleanup()` 使用本地 `System.currentTimeMillis()` 减去 `2*getMaxUpdateInterval()` 作为删除阈值，只处理临时实例；若节点系统时间与数据库估算时钟存在漂移，清理窗口会提前或延后，需结合心跳日志核实阈值。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/naming/SysDaoNamingService.java†L52-L80】
- `SysDaoResourceLockManager` 将资源 ID 拆分为锁组/锁名复合主键，`tryLockWithLease` 在数据库写入锁记录失败时会轮询剩余等待时间、尝试删除过期记录并重试；`tryResetLease` 使用数据库估算时钟构造新的 `expireAt` 并将 `version` 自增后再提交，`getLockState`/`releaseLock` 通过新 Session 读取或直接删除实体，确保租约续期和释放流程不会命中当前事务缓存。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/SysDaoResourceLockManager.java†L69-L233】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/EntityResourceLockState.java†L7-L44】
- `isHoldingLock` 会通过 `getLockState` 启动新 Session 重新读取数据库行，比对锁持有者与版本号，只有完全匹配才视为仍持有锁；`releaseLock` 则在 `runLocal` 事务内直接删除 `EntityResourceLockState` 中缓存的实体，实现释放时无需额外构造 SQL 并避免当前事务缓存污染。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/SysDaoResourceLockManager.java†L204-L233】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/EntityResourceLockState.java†L7-L44】
- `SysDaoNamingService.getMaxUpdateInterval()` 会在自定义 `autoUpdateInterval` 基础上额外增加 1000ms 安全裕度；未配置时默认心跳窗口为 60 秒，后续 `getServices`/`getInstances` 与 `cleanup` 均依赖该阈值过滤或删除节点。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/naming/SysDaoNamingService.java†L44-L123】
- `SysDaoLeaderElector` 在 `checkElection` 中最多循环 20 次确认角色，leader 分支在安全间隔内调用 `refreshLeader` 更新 `expireAt`，接近超时时通过 `tryRenewLease` 配合 `orm_disableVersionCheckError(true)` 调整租约并依据 `orm_readonly()` 判断是否成功；follower 分支在 leader 缺席或超时时使用 `tryBecomeLeader`/`changeLeader` 提升 Epoch 并刷新 `expireAt` 与 `leaderAdder`，出现异常或 Epoch 不符则触发 `onBecomeFollower` + `onRestartElection`。`restartElection` 最多 10 次把数据库 Epoch+1、`expireAt` 设为当前时间，并通过 `ormTemplate.runInNewSession` 读取最新的 leader 行避免复用当前 Session 缓存，迫使所有节点重新选举。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/elector/SysDaoLeaderElector.java†L44-L236】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/elector/SysDaoLeaderElector.java†L219-L243】
- `_NopSysDaoConstants` 定义事件状态枚举，供消息服务和实体默认值引用，确保状态机逻辑与常量一致。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/_NopSysDaoConstants.java†L4-L24】
- `SysDaoMessageService` 启动时注册两个定时任务分别处理广播与非广播事件；`updateScheduleTime` 在事务内依据重试策略和最小延迟调整调度时间并乐观锁更新；`doProcessEvent` 成功时写入 `SYS_EVENT_STATUS_PROCESSED`，异常时依赖 `RetryPolicy` 决定重试或标记失败；发送 API 创建新事件行并立即持久化。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L45-L312】
- `SysDaoMessageService` 默认以 `minProcessDelay=10000`（10 秒）约束重试计划，`updateScheduleTime` 对重试策略返回的延迟执行 `Math.max(minProcessDelay, delay)`，而默认 IoC Bean `nopSysDaoMessageService` 并未覆盖该属性，需通过差量 `<property>` 或配置注入自定义值。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L69-L99】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L150-L198】【F:nop-sys/nop-sys-dao/src/main/resources/_vfs/nop/sys/beans/app-dao.beans.xml†L31-L36】
- `SysDaoMessageService` 默认实例化 `RetryPolicy`，其 `maxRetryCount=2`、初始延迟 1s、指数回退与 0.3 抖动均启用，超过最大重试次数或被异常判定为不可恢复时会返回 `-1` 终止调度。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L60-L90】【F:nop-commons/src/main/java/io/nop/commons/util/retry/RetryPolicy.java†L17-L104】【F:nop-commons/src/main/java/io/nop/commons/util/retry/RetryPolicy.java†L123-L180】
- `SysDaoMessageService` 在启动阶段若未显式注入线程池或定时器，会回退到 `GlobalExecutors`，并以 `checkInterval=500ms`、`fetchSize=100`、`startGap=5000ms` 初始化轮询节奏；广播调度通过 `startTime=currentTime-startGap` 补偿冷启动期间的事件，`LocalMessageService` 复用本地订阅表并桥接 `send` 调用。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L51-L134】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L200-L248】
- `SysDaoMessageService.processBroadcastEvent` 将数据库事件转换为 `ApiRequest` 后交给 `LocalMessageService.invokeMessageListener`，后者依据 `MessageCoreConstants.TOPIC_PREFIX_BROADCAST` 区分主题并在消费结果返回时通过 `getAckTopic` 将响应写入 `ack-` 前缀主题，实现数据库队列与本地订阅的双向桥接。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L205-L235】【F:nop-message/nop-message-core/src/main/java/io/nop/message/core/local/LocalMessageService.java†L123-L200】【F:nop-api-core/src/main/java/io/nop/api/core/message/IMessageSubscriber.java†L10-L24】
- `LocalMessageService.invokeMessageListener` 支持监听器同步返回值或 `CompletionStage` 异步完成；异步场景下会在 `whenComplete` 回调中调用 `handleMessageResult`，成功时写入 `ack-` 主题，失败则输出 `nop.message.consumer-error` 日志，说明应答逻辑会等待异步任务结束且仅在本地内存发送响应，不会持久化到数据库；若回调抛出异常，消息只保留在原事件表，未发送任何应答。【F:nop-message/nop-message-core/src/main/java/io/nop/message/core/local/LocalMessageService.java†L152-L200】
- `fetchBroadcastEvents` 根据广播订阅主题拼装 `QueryBean`，要求 `eventTime>startTime` 且 `isBroadcast=true`，并调用 `dao.findNext(lastBroadcastEvent, …, fetchSize)` 按 `eventId` 顺序推进，同时更新 `lastBroadcastEvent` 作为下次窗口起点。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L219-L235】
- `fetchNonBroadcastEvents` 仅在存在非广播监听器时拉取消息，过滤 `eventStatus=WAITING` 与 `isBroadcast=false`，按 `processTime` 升序分页，并配合 `updateScheduleTime` 的乐观锁返回值让未更新成功的事件在下一轮重试。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L137-L165】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L238-L253】
- `SysEventHelper` 在从事件转回 `ApiRequest` 时恢复字段选择、头信息、业务键和时间戳；写入事件时自动设置主题、是否广播、业务标识和 JSON 化的请求头/数据，使数据库行可完整还原原始消息。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysEventHelper.java†L17-L68】
- `NopSysCheckerRecord` 与 `NopSysMakerCheckerRecord` 实体保存审批请求与 MakerChecker 执行结果，持久化业务对象名、动作、请求载荷、try 结果、回调状态、审批人、输入页面等字段，可配合 `makerId`、`requestTime`、`status` 字段重放审批流程。【F:nop-sys/nop-sys-dao/src/main/resources/_vfs/nop/sys/orm/_app.orm.xml†L206-L276】【F:nop-sys/nop-sys-meta/src/main/resources/_vfs/nop/sys/model/NopSysCheckerRecord/_NopSysCheckerRecord.xmeta†L1-L74】
- `GraphQLConfigs.CFG_GRAPHQL_MAKER_CHECKER_ENABLED` 默认返回 `Boolean.FALSE`，GraphQL WebService 会把该开关注入执行上下文，只有当配置明确设为 true 且 Provider 判定启用时才会改走审批链，未启用时保持原始 mutate 行为。【F:nop-graphql/nop-graphql-core/src/main/java/io/nop/graphql/core/GraphQLConfigs.java†L33-L44】【F:nop-graphql/nop-graphql-core/src/main/java/io/nop/graphql/core/web/GraphQLWebService.java†L70-L107】
- `MakerCheckerTryServiceAction` 会在 try 方法返回（同步或 `CompletionStage`）后封装 `SendForCheckRequest`，把 `ApiRequest` 数据、try 结果、制单人信息写入请求，并调用 `ApiResponse.buildSuccess(reqId)` 将审批编号包装在 GraphQL 响应中。【F:nop-biz/src/main/java/io/nop/biz/makerchecker/MakerCheckerTryServiceAction.java†L24-L67】【F:nop-biz/src/main/java/io/nop/biz/makerchecker/SendForCheckRequest.java†L17-L66】
- `CrudBizModel` 在 `save`/`update`/`delete` 上应用 `@BizMakerChecker` 注解，`ObjectDefinitionExtProcessor` 会验证 try/cancel 方法存在并包裹为 `MakerCheckerTryServiceAction`，后者构造 `SendForCheckRequest`（含请求数据、try 结果、制单人信息）并交给 `IMakerCheckerProvider` 异步写入审批表。【F:nop-biz/src/main/java/io/nop/biz/crud/CrudBizModel.java†L504-L520】【F:nop-biz/src/main/java/io/nop/biz/crud/CrudBizModel.java†L763-L789】【F:nop-biz/src/main/java/io/nop/biz/crud/CrudBizModel.java†L954-L989】【F:nop-biz/src/main/java/io/nop/biz/impl/ObjectDefinitionExtProcessor.java†L61-L83】【F:nop-biz/src/main/java/io/nop/biz/makerchecker/MakerCheckerTryServiceAction.java†L32-L67】【F:nop-biz/src/main/java/io/nop/biz/makerchecker/SendForCheckRequest.java†L17-L66】【F:nop-biz/src/main/java/io/nop/biz/makerchecker/IMakerCheckerProvider.java†L15-L36】
- GraphQL WebService 每次执行请求时根据 `nop.graphql.maker-checker.enabled` 配置将 MakerChecker 状态注入执行上下文，使得服务端可以统一切换审批流程；BizObject 构建阶段同样依赖 `IMakerCheckerProvider` 在 GraphQL 字段上保留 try/cancel 元信息。【F:nop-graphql/nop-graphql-core/src/main/java/io/nop/graphql/core/GraphQLConfigs.java†L40-L42】【F:nop-graphql/nop-graphql-core/src/main/java/io/nop/graphql/core/web/GraphQLWebService.java†L70-L107】【F:nop-biz/src/main/java/io/nop/biz/impl/BizObjectBuilder.java†L70-L158】
- `OrmEntityChangeLogInterceptor` 拦截带有 `audit`/`audit-save` 标签的实体操作，针对保存场景复制列值写入 `NopSysChangeLog`，更新场景仅遍历脏属性记录旧值/新值，删除时写入 `PROP_DELETED` 标记，同时携带操作人、业务键、审批人等上下文信息，默认排除版本字段与 `no-audit` 列。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/log/OrmEntityChangeLogInterceptor.java†L13-L94】

## 4. 推测（Hypotheses）
- ⚠️ 需要确认官方是否已有补丁或 Issue 追踪 `SysDaoResourceLockManager.isExpired` 比较符号反转问题，并评估在修复发布前的缓解方案（如串行化调用或外部互斥服务）。→计划：检索提交历史/Issue，形成缺陷描述草稿并列出临时规避建议。
- ⚠️ `SysDaoMessageService` 的 `minProcessDelay` 默认 10 秒，即使重试策略返回更小延迟也会被提升；当前 IoC 默认 Bean 未配置该属性，仍需在实际部署中确认差量 `<property>` 或配置项的覆盖示例，以便评估重试实时性。→计划：搜集 `_delta`/配置样例，记录覆盖写法与默认值影响。
- ⚠️ `SysDaoNamingService.cleanup` 只针对临时实例执行删除，持久实例若宕机不会被自动清理，需结合实际部署策略确认是否依赖外部健康检查来维护持久节点。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/naming/SysDaoNamingService.java†L72-L123】→计划：查找配置或运行手册确认持久实例下线流程。
- ⚠️ 仓库仅提供 `IMakerCheckerProvider` 接口与 GraphQL 挂载逻辑，默认 Bean 未见实现，推测具体落库流程需由业务实现自定义 Provider；需确认官方是否在其他模块或部署脚本中注入实现，以免 MakerChecker 流程被动关闭。→计划：搜检 `nop-sys-service`、`nop-auth` 等模块的 Bean 定义或部署文档，明确默认 Provider 来源。【F:nop-biz/src/main/java/io/nop/biz/makerchecker/IMakerCheckerProvider.java†L15-L36】【F:nop-biz/src/main/java/io/nop/biz/impl/BizObjectBuilder.java†L70-L158】

## 5. 规则（IF-THEN，可执行）
1. IF `CFG_SYS_INIT_DEFAULT_SEQUENCE` 仍为默认 true 且 `nop_sys_sequence` 为空 THEN `SysSequenceGenerator.lazyInit` 会在租户 `0` 插入 `default` 序列，避免首次请求失败。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/NopSysDaoConfigs.java†L15-L18】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L138-L175】
2. IF `NopSysCodeRule` 缺少 `seqName` THEN `SysCodeRuleGenerator.generate` 会抛出 `ERR_SYS_CODE_RULE_EMPTY_SEQ_NAME`，调用方必须补齐序列配置或捕获异常。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/coderule/SysCodeRuleGenerator.java†L62-L66】
3. IF 字典名以 `sys/` 开头且当前租户已注入 THEN `SysDictLoader.existsDict` 会执行数据库 Exists 校验，确保引用的系统字典真实存在。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/dict/SysDictLoader.java†L82-L90】
4. IF 数据库中已存在锁记录且 `isExpired` 判断过期成功 THEN `tryLockWithLease` 会尝试删除旧记录并继续重试；否则线程将每 100ms 睡眠直至超时或成功获取锁。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/SysDaoResourceLockManager.java†L119-L172】
5. IF 持有者需要延长锁租期 THEN 必须在租约到期前调用 `tryResetLease`，该方法使用数据库估算时钟重新计算 `expireAt` 并将版本号自增；调用者需以返回值判断续约是否成功，并在成功后刷新本地 `IResourceLockState`，否则旧版本会导致后续释放/续约失败。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/SysDaoResourceLockManager.java†L200-L219】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/EntityResourceLockState.java†L7-L44】
6. IF `SysDaoMessageService` 处理事件失败且重试策略返回负值 THEN 事件状态改为 `SYS_EVENT_STATUS_FAILED`，否则更新重试次数并推迟调度时间。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L189-L198】
7. IF 需要把消息重试延迟缩短到 10 秒以下 THEN 必须在差量 Bean（如 `_delta/nop/sys/beans/app-dao.beans.xml`）或配置中心为 `nopSysDaoMessageService` 设置 `<property name="minProcessDelay" value="…"/>`，否则 `updateScheduleTime` 会使用默认的 `Math.max(10000, delay)` 覆盖更小的重试间隔。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L150-L198】【F:nop-sys/nop-sys-dao/src/main/resources/_vfs/nop/sys/beans/app-dao.beans.xml†L31-L36】
8. IF 竞争线程基于默认实现轮询数据库锁 THEN `isExpired` 的 `>=` 比较会将仍在租期内的记录视为过期并执行 `orm().tryDelete(existing)`，造成锁持有者在无日志提示的情况下丢失互斥，需要在修复前通过外部互斥或序列化访问规避。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/SysDaoResourceLockManager.java†L147-L177】
9. IF 期望消息重试次数超过默认的 2 次或需要线性退避 THEN 必须为 `SysDaoMessageService` 注入自定义 `RetryPolicy`（例如差量 Bean 设置 `<property name="retryPolicy" ref="..."/>`），否则默认策略会在 `maxRetryCount=2` 后返回 `-1` 终止调度并跳出重试循环。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L60-L90】【F:nop-commons/src/main/java/io/nop/commons/util/retry/RetryPolicy.java†L17-L180】
10. IF 未覆盖轮询线程池、定时器或窗口参数 THEN `SysDaoMessageService.doStart` 会回退到全局执行器，并使用 `checkInterval=500ms`、`fetchSize=100`、`startGap=5000ms` 的默认组合；需要扩大批量或延长广播补偿窗口时，必须通过 `<property>` 显式设置对应属性。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L51-L134】
11. IF 需要补偿启动前遗漏的广播消息 THEN 应保持或放大 `startGap` 并在切换节点或清空缓存时重置 `lastBroadcastEvent`，因为 `fetchBroadcastEvents` 依赖 `eventTime>startTime` 与 `findNext(lastBroadcastEvent, …)` 顺序遍历窗口，窗口过小或指针未重置都会跳过历史事件。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L119-L135】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L219-L235】
12. IF `updateScheduleTime` 因乐观锁仅更新部分行 THEN 未命中的事件会继续保持 `eventStatus=WAITING` 并由下一轮 `fetchNonBroadcastEvents` 重新获取，因此需要通过日志监控重复回流的事件并评估是否延长 `fetchSize` 或排查版本冲突。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L137-L165】
13. IF `LocalMessageService` 的监听器返回 `Acknowledge`、普通对象或 `CompletionStage` THEN 成功结果会在当前线程或 `whenComplete` 回调中调用 `getAckTopic(topic)` 写入 `ack-` 前缀主题，订阅方需同时监听业务主题与响应主题才能得到同步/异步应答。【F:nop-message/nop-message-core/src/main/java/io/nop/message/core/local/LocalMessageService.java†L152-L200】【F:nop-api-core/src/main/java/io/nop/api/core/message/IMessageSubscriber.java†L10-L24】
14. IF 期望跨节点消费 `ack-` 响应 THEN 必须显式在本地订阅 `ack-<topic>` 或转发到外部消息总线，因为 `LocalMessageService.send` 只在内存中分发，不会将应答持久化到数据库或系统事件表。【F:nop-message/nop-message-core/src/main/java/io/nop/message/core/local/LocalMessageService.java†L140-L200】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L205-L235】
15. IF `CompletionStage` 在 `whenComplete` 中抛出异常 THEN `LocalMessageService` 仅记录 `nop.message.consumer-error` 日志且不会写入 `ack-` 主题，调用方需结合业务重试或人工补偿，否则上游无法收到应答。【F:nop-message/nop-message-core/src/main/java/io/nop/message/core/local/LocalMessageService.java†L158-L200】
16. IF `SysDaoLeaderElector` 发现数据库记录的 leader 与本节点不一致、Epoch 大于当前或续约失败 THEN 会调用 `onBecomeFollower` 并触发 `onRestartElection`，确保节点降级后重新参与选举并避免脑裂。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/elector/SysDaoLeaderElector.java†L82-L108】
17. IF 需要强制所有节点重新感知 leader 状态 THEN `restartElection` 会在最多 10 次循环中将 `leaderEpoch` 加一并把 `expireAt` 设置为当前时间，驱动现任 leader 重新校验并触发新一轮选举。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/elector/SysDaoLeaderElector.java†L219-L236】
18. IF 依赖 `restartElection` 强制重选 THEN 必须保留 `ormTemplate.runInNewSession` 的新会话读取行为，确保在当前事务已缓存老记录时仍能取回数据库中的最新 leader 行并正确提升 Epoch。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/elector/SysDaoLeaderElector.java†L219-L243】
19. IF 服务实例标记为临时且长时间未调用 `registerInstance`/`updateInstance` 刷新心跳 THEN `cleanup` 会在 `cleanupInterval` 调度中根据 `2*getMaxUpdateInterval()` 删除该记录，因此需确保续约周期小于设定心跳上限或改为持久实例以免被自动清理。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/naming/SysDaoNamingService.java†L44-L123】
20. IF 期待注册中心自动清理过期实例 THEN 需在差量或配置中心为 `SysDaoNamingService` 设置 `cleanupInterval`，否则 `@PostConstruct` 不会注册定时任务，临时实例即使超出心跳窗口也不会被删除。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/naming/SysDaoNamingService.java†L52-L80】
21. IF 实体模型配置 `orm:audit` 或 `orm:audit-save` 标签 THEN `OrmEntityChangeLogInterceptor` 会在 `postSave`/`postUpdate`/`postDelete` 中写入 `NopSysChangeLog`，并自动忽略版本字段及带 `no-audit` 标签的列；若实体本身即为 `NopSysChangeLog` 则不会递归记录，避免日志自增循环。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/log/OrmEntityChangeLogInterceptor.java†L18-L94】
22. IF 在配置源中显式设置 `nop.orm.audit.enabled=false` THEN `nopOrmEntityChangeLogInterceptor` 的 `feature:on` 条件会失败，审计拦截器不会注册到 IoC，所有实体即使带 `audit` 标签也不会写入 `NopSysChangeLog`，需在多环境配置时确认是否按需开启。【F:nop-sys/nop-sys-dao/src/main/resources/_vfs/nop/sys/beans/app-dao.beans.xml†L31-L36】
23. IF 模型只声明 `orm:audit-save` 而未配置 `orm:audit` THEN 更新与删除分支会直接返回，导致审计缺口；需要在需要全量追踪的实体上补齐 `orm:audit` 标签，并使用 `orm:no-audit` 排除无需记录的列。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/log/OrmEntityChangeLogInterceptor.java†L18-L94】【F:nop-sys/nop-sys-dao/src/main/resources/_vfs/nop/sys/orm/_app.orm.xml†L1-L622】
24. IF `nop.graphql.maker-checker.enabled` 打开且某 BizAction 使用 `@BizMakerChecker` 且 Provider 判定启用 THEN GraphQL 执行器会调用绑定的 `MakerCheckerTryServiceAction` 执行 try 方法、构造 `SendForCheckRequest` 并交由 `IMakerCheckerProvider.sendForCheckAsync` 持久化审批请求，原操作返回审批编号而不是直接改写数据库。【F:nop-graphql/nop-graphql-core/src/main/java/io/nop/graphql/core/GraphQLConfigs.java†L33-L44】【F:nop-graphql/nop-graphql-core/src/main/java/io/nop/graphql/core/web/GraphQLWebService.java†L70-L107】【F:nop-graphql/nop-graphql-core/src/main/java/io/nop/graphql/core/engine/GraphQLExecutor.java†L205-L224】【F:nop-biz/src/main/java/io/nop/biz/crud/CrudBizModel.java†L504-L520】【F:nop-biz/src/main/java/io/nop/biz/crud/CrudBizModel.java†L763-L989】【F:nop-biz/src/main/java/io/nop/biz/impl/ObjectDefinitionExtProcessor.java†L61-L83】【F:nop-biz/src/main/java/io/nop/biz/makerchecker/MakerCheckerTryServiceAction.java†L20-L66】【F:nop-biz/src/main/java/io/nop/biz/makerchecker/IMakerCheckerProvider.java†L15-L36】
25. IF `nop.graphql.maker-checker.enabled` 保持默认 false 或 Provider 判定不启用 THEN `GraphQLExecutor` 直接执行原始 `IDataFetcher`，不会创建 `SendForCheckRequest`、`NopSysCheckerRecord` 行或改写返回值，Mutate 行为与未开启审批时一致。【F:nop-graphql/nop-graphql-core/src/main/java/io/nop/graphql/core/GraphQLConfigs.java†L33-L44】【F:nop-graphql/nop-graphql-core/src/main/java/io/nop/graphql/core/engine/GraphQLExecutor.java†L205-L236】
26. IF try 方法返回 `CompletionStage` 或 `ApiResponse` THEN `MakerCheckerTryServiceAction` 会在 `CompletionStage.thenCompose` 中等待执行完成，并通过 `toApiRequest`/`toTryResponse` 填充请求数据，最终调用 `ApiResponse.buildSuccess(reqId)` 把审批编号封装为成功响应交给 GraphQL，确保同步/异步 try 方法行为一致。【F:nop-biz/src/main/java/io/nop/biz/makerchecker/MakerCheckerTryServiceAction.java†L24-L66】
27. IF 为 `SysDaoNamingService` 配置 `autoUpdateInterval` THEN 实际心跳窗口为该值再加 1000ms 安全裕度，所有实例必须在此阈值内调用 `registerInstance`/`updateInstance` 才能通过 `getServices`/`getInstances` 过滤并避免被 `cleanup` 判定过期。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/naming/SysDaoNamingService.java†L44-L123】
28. IF 运行节点与数据库时间存在偏差 THEN `registerInstance` 仍会使用 `dao.getDbEstimatedClock().getMaxCurrentTime()` 更新 `updateTime`，`getServices` 也通过 `clock.getMinCurrentTimeMillis()` 计算过滤阈值，需确保数据库时间同步准确，否则实例可能被误判为过期或长时间留存。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/naming/SysDaoNamingService.java†L72-L110】
29. IF 节点本地时钟明显滞后于数据库 THEN `getInstances` 基于 `System.currentTimeMillis()-getMaxUpdateInterval()` 的过滤条件可能误将仍在续约窗口内的实例视为过期，或在节点时钟超前时延迟剔除过期实例，因此需要在部署前校准所有应用节点的系统时间或改用集中时间源。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/naming/SysDaoNamingService.java†L110-L163】
30. IF 注册的 `ServiceInstance` metadata 或标签字符串超过列长度（JSON 1000 字符、标签 100 字符） THEN 数据库会因超长字段抛出异常或截断信息，导致实例注册失败或标签丢失；需在客户端限制 metadata 规模并按需拆分标签字段。【F:nop-sys/nop-sys-dao/src/main/resources/_vfs/nop/sys/orm/_app.orm.xml†L553-L589】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/naming/SysDaoNamingService.java†L103-L163】
31. IF 应用节点系统时间明显超前或滞后于数据库估算时钟 THEN `cleanup()` 以本地 `System.currentTimeMillis()` 推导的删除阈值会提前剔除仍在续约窗口内的临时实例，或延迟清理已过期节点；部署前需同步各节点时钟或监控心跳日志评估漂移影响。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/naming/SysDaoNamingService.java†L52-L123】
32. IF 业务在长事务或异步场景中需要确认锁仍由自己持有 THEN 应调用 `isHoldingLock` 重新读取数据库记录并比对持有者与版本；若返回 false 或准备释放锁，应先刷新本地 `IResourceLockState` 再调用 `releaseLock`，否则由于版本不一致或实体复用可能导致释放失败或误删他人锁。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/SysDaoResourceLockManager.java†L200-L233】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/EntityResourceLockState.java†L7-L44】
33. IF `tryLockWithLease` 在等待窗口内始终未能 `saveNew` 成功 THEN `leftWait<=0` 分支会返回 `null` 并记录 `nop.lock.try-lock-fail` 日志，调用方必须将其视为锁获取失败并根据业务重试或降级，而不是继续依赖未初始化的 `IResourceLockState`。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/SysDaoResourceLockManager.java†L101-L143】【F:nop-commons/src/main/java/io/nop/commons/concurrent/lock/impl/ResourceLock.java†L22-L86】
34. IF 需要人工释放疑似遗留锁 THEN 应在确认业务已放弃后再调用 `forceUnlock`，因为该方法直接 `deleteById` 不做版本或持有者校验，会强制删除 `nop_sys_lock` 行并可能打断仍在执行的任务。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/SysDaoResourceLockManager.java†L90-L99】
35. IF 需要在多网卡或容器场景下准确标识锁持有节点 THEN 应在 IoC 中为 `SysDaoResourceLockManager` 注入自定义 `IServerAddrFinder`，否则 `saveNew` 会回退 `NetHelper.findLocalIp()` 填充 `holderAdder`，导致记录的地址可能随容器重启变化或暴露宿主节点，对调试定位不利。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/SysDaoResourceLockManager.java†L42-L67】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/SysDaoResourceLockManager.java†L179-L197】
36. IF 未在调用处显式传入等待/租期并希望统一放大互斥窗口 THEN 应通过 `<property name="defaultWaitTime/defaultLeaseTime">` 覆盖 `SysDaoResourceLockManager` 的 Bean 属性，使 `getLock()` 与 `ResourceLock` 使用新的默认值；否则仍会回退到 1 秒等待/10 秒租期，长事务易在默认窗口内超时返回 `null`。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/SysDaoResourceLockManager.java†L38-L123】
37. IF 序列缓存耗尽触发 `syncFromDb` THEN `runLocal` 会在全新 Session 和 `REQUIRES_NEW` 事务中锁定并更新 `NopSysSequence`，即使外层业务事务回滚，序列的 `nextValue` 也不会回退，因此在跨事务场景要提前规划补偿或容忍跳号。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L178-L239】
38. IF 运行期补齐数据库序列后希望立刻切换到递增值 THEN 除了调用 `removeCache(seqName)` 失效主缓存，还需等待 `defaultCache` 60 秒 TTL 或重启进程，因为公开的清理方法不会移除 fallback 缓存中的 `SeqItem` 占位；若在 TTL 内继续请求仍会返回旧值或随机 UUID，需要在运维手册中规划缓存失效窗口。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L52-L110】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L206-L239】
39. IF 未显式配置 `nop.sys.seq.snowflake-worker-id` THEN `afterPropertiesSet` 会读取 `CFG_HOST_ID`，为空时回退 `NetHelper.findLocalIp()`，再以 `HashHelper.murmur3_32(hostId) % 1024` 生成稳定 workerId，因此必须确保 hostId/IP 在同一节点不变以避免雪花序列跨重启漂移或多节点碰撞。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L126-L135】
40. IF 多节点部署需要显式划分 snowflake workerId THEN 应通过配置源或差量为 `SysSequenceGenerator` 设置 `nop.sys.seq.snowflake-worker-id`，因为 `setSnowflakeWorkerId` 的 `@InjectValue` 会覆盖默认哈希，避免不同 hostId 哈希碰撞或容器重启导致的 workerId 漂移。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L104-L108】【F:nop-api-core/src/main/java/io/nop/api/core/config/AppConfig.java†L47-L60】
41. IF 通过 `SysDaoMessageService.sendAsync` 提交的是 `ApiRequest` 消息 THEN `SysEventHelper.toSysEvent` 会复制 headers、selection、data、`bizKey`/`bizObjName` 并基于 `bizObjName|bizKey` 计算稳定的 `partitionIndex`，否则 `sendAsync` 会使用 `MathHelper.random()` 生成随机分区并仅存储消息类名，业务若需要幂等顺序应确保带 `ApiRequest` 元数据并设置 `ApiHeaders`。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L266-L304】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysEventHelper.java†L19-L69】
42. IF 序列未在数据库配置且调用方设置 `useDefault=true` THEN `findSeqItem` 会回退 `default` 序列并重用其缓存；若 `useDefault=false` 则创建 `useUuid=true` 的占位项，后续 `generateLong/String` 返回随机值，需确保关键业务显式配置序列或开启默认回退。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L178-L269】
43. IF 在运行期调整数据库序列配置或新建序列 THEN 必须调用 `SysSequenceGenerator.removeCache(seqName)` 或 `clearCache()` 清理主缓存，并知晓 `defaultCache` 无对外清除接口，只能等待 60 秒 TTL 或重启进程，否则 JVM 会继续复用旧的 `SeqItem`/占位项并返回缓存值或随机 UUID，导致新配置延迟生效。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L52-L214】
44. IF 外层业务在一个事务中获取序列后又显式回滚 THEN `SysSequenceGenerator.runLocal(syncFromDb)` 依旧会在独立 Session 与 `REQUIRES_NEW` 事务里提交 `nextValue` 的前移，回滚不会撤销已写入的序列值，调用方需提前接受跳号或在业务层设计补偿逻辑。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L206-L239】
45. IF 默认序列由 `lazyInit` 自动插入 THEN 该记录会以 `cacheSize=100`、`stepSize=1`、`seqType='seq'` 固定批量与步长，后续若要缩放缓存批次必须更新 `nop_sys_sequence` 表并刷新 JVM 缓存，否则 `syncFromDb` 仍按 100 个步长消费批量，可能在批量很大时造成值跳跃过多。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L146-L175】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L206-L239】
46. IF 数据库把 `step_size` 设为 0 或负数 THEN `SeqItem.update` 会在装载时自动把步长重置为 1，并在 `cacheSize=0` 时退化为逐次从数据库取号；若需要非 1 步长必须在数据库正确配置正整数并配合 `cacheSize` 评估跳号范围。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L62-L206】
47. IF 未为 `SysDaoMessageService` 注入专用线程池或定时器 THEN `doStart` 会回退 `GlobalExecutors.globalWorker()/globalTimer()` 并以 `startTime=currentTime-startGap` 启动广播/非广播轮询，因此覆盖调度线程时需一并配置 `timer/executor` 以避免与全局线程池争用资源。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L63-L118】
48. IF 同一毫秒内雪花序列耗尽 12bit 序列空间 THEN `SnowflakeSequenceGeneator` 会在 `sequence==0` 时调用 `MathHelper.secureRandom().nextInt(100)` 重置序列并等待下一毫秒；进入新毫秒同样随机起始序列值，并在时钟回拨时通过 `tilNextMillis` 自旋等待，确保 ID 不重复且分散热点。【F:nop-dao/src/main/java/io/nop/dao/seq/SnowflakeSequenceGeneator.java†L76-L129】

## 6. 流程（Text-Sequence）
1. **序列生成**：加载/缓存 `SeqItem` → 若配置雪花或 UUID 则直接返回 → 否则判断缓存剩余 → 缓存不足时进入事务 `runLocal` 查询 `NopSysSequence`、锁定行、计算下一 `nextValue` 并更新数据库 → 返回当前值。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L178-L206】
2. **编码生成**：根据规则名查询 `NopSysCodeRule` → 校验 `seqName` 非空 → 取得系统时间与序列生成器 → 调用 `ICodeRule.generate` 产生编码并返回。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/coderule/SysCodeRuleGenerator.java†L52-L68】
3. **字典加载**：`DictProvider` 触发 `SysDictLoader.loadDict` → 构造 `QueryBean` 关联字典与选项 → 读取并映射为 `DictOptionBean` 列表 → 返回 `DictBean` 交给调用方缓存。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/dict/SysDictLoader.java†L58-L77】
4. **服务注册**：应用调用 `registerInstance` → 若存在记录则更新 metadata 与 `updateTime`，否则新建实体 → 可选定时任务 `cleanup` 删除过期临时节点 → `getServices`/`getInstances` 依据最近更新时间过滤有效实例。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/naming/SysDaoNamingService.java†L72-L163】
5. **数据库锁获取**：`getLock` 生成锁对象 → `tryLockWithLease` 写入 `NopSysLock`，失败则循环校验过期、删除旧记录、休眠重试，成功后返回实体包装；持有方续约时调用 `tryResetLease`、释放前可通过 `isHoldingLock` 复核持有者/版本，再执行 `releaseLock` 删除行。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/SysDaoResourceLockManager.java†L69-L233】
6. **消息调度**：服务启动调用 `doStart` → 定时任务轮询广播/非广播事件 → 非广播先调用 `updateScheduleTime` 乐观锁更新调度，再执行监听器 → 成功标记 `PROCESSED`，失败根据策略重试或置 `FAILED`。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L112-L198】
7. **消息分发与应答桥接**：`processBroadcastEvent`/`fetchBroadcastEvents` 拉取数据库事件 → 转换为 `ApiRequest` 后交给 `LocalMessageService.invokeMessageListener` → 监听器执行并返回值 → `handleMessageResult` 依据返回内容决定是否向 `ack-` 前缀主题发送应答，形成数据库事件→本地订阅→响应主题的闭环。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L205-L235】【F:nop-message/nop-message-core/src/main/java/io/nop/message/core/local/LocalMessageService.java†L164-L200】【F:nop-api-core/src/main/java/io/nop/api/core/message/IMessageSubscriber.java†L10-L24】
8. **实体审计日志写入**：业务实体触发 `postSave`/`postUpdate`/`postDelete` → 拦截器首先判定 `nop.orm.audit.enabled` 是否为真并检查模型标签（`audit`/`audit-save`/`no-audit`）与版本字段 → 克隆基础日志信息（操作人、应用、业务键、审批人） → 保存场景按列复制新值、更新场景遍历脏属性写入旧值/新值、删除场景写入 `PROP_DELETED` 标记 → 通过 `IEntityDao<NopSysChangeLog>` 写入 `NopSysChangeLog` 表供审计追踪。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/log/OrmEntityChangeLogInterceptor.java†L18-L94】【F:nop-sys/nop-sys-dao/src/main/resources/_vfs/nop/sys/beans/app-dao.beans.xml†L31-L36】
9. **选主轮询**：定时触发 `checkElection`（最多循环 20 次避免饥饿）→ leader 分支校验数据库记录与 Epoch，一致则调用 `refreshLeader` 延长租约，接近超时时执行 `tryRenewLease` 重置 `expireAt`；若续约失败或 Epoch 不符则降级并触发 `onRestartElection` → follower 分支在无 leader 时 `tryBecomeLeader` 插入新记录，或在检测到记录超时/指向自己时调用 `changeLeader`（内部提升 Epoch 并刷新租约/地址）→ 选举完成后调用 `onElectionCompleted` 广播结果。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/elector/SysDaoLeaderElector.java†L44-L216】
10. **Run-less 消息延迟校准**：准备事件样本（含 `retryTimes`、`eventStatus`、`scheduleTime`）→ 根据 `getRetryDelay` 的策略输出计算 `delay` → 对照 `Math.max(minProcessDelay, delay)` 推导理论调度时间 → 将计算值填入调试表格并比对 `clock.getMaxCurrentTimeMillis()` 推导的下一次执行时间，确认默认 10 秒是否被覆盖或放大。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L150-L198】
11. **MakerChecker 审批链**：GraphQLWebService 根据 `nop.graphql.maker-checker.enabled` 标志构建执行上下文（默认 false 需显式开启）→ `ObjectDefinitionExtProcessor` 为带 `@BizMakerChecker` 的操作装配 `MakerCheckerTryServiceAction` → 执行器先调用 try 方法验证请求（支持同步或 `CompletionStage`）→ `MakerCheckerTryServiceAction` 构造 `SendForCheckRequest`（含业务对象、动作、请求数据、try 结果、制单人）并调用 `IMakerCheckerProvider.sendForCheckAsync` → Provider 负责把请求写入 `NopSysCheckerRecord`/`NopSysMakerCheckerRecord`，返回值通过 `ApiResponse.buildSuccess(reqId)` 封装审批编号供前端提示。【F:nop-graphql/nop-graphql-core/src/main/java/io/nop/graphql/core/GraphQLConfigs.java†L33-L44】【F:nop-graphql/nop-graphql-core/src/main/java/io/nop/graphql/core/web/GraphQLWebService.java†L70-L107】【F:nop-graphql/nop-graphql-core/src/main/java/io/nop/graphql/core/engine/GraphQLExecutor.java†L205-L224】【F:nop-biz/src/main/java/io/nop/biz/impl/ObjectDefinitionExtProcessor.java†L61-L83】【F:nop-biz/src/main/java/io/nop/biz/makerchecker/MakerCheckerTryServiceAction.java†L20-L66】【F:nop-biz/src/main/java/io/nop/biz/makerchecker/SendForCheckRequest.java†L17-L66】【F:nop-sys/nop-sys-dao/src/main/resources/_vfs/nop/sys/orm/_app.orm.xml†L206-L276】

## 7. 状态机（Transition Table）
| Current | Event | Guard | Next | Side Effects |
| --- | --- | --- | --- | --- |
| WAITING (`0`) | 监听器处理成功 | - | PROCESSED (`10`) | 更新 `eventStatus=10`、写回 `processTime`。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L167-L178】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/_NopSysDaoConstants.java†L4-L24】|
| WAITING (`0`) | 监听器抛异常且策略允许重试 | `getRetryDelay` ≥ 0 | WAITING (`0`) | 递增 `retryTimes`、推迟 `scheduleTime` 至当前时间 + 延迟。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L189-L197】|
| WAITING (`0`) | 监听器抛异常且策略拒绝重试 | `getRetryDelay` < 0 | FAILED (`30`) | 标记 `eventStatus=30`，保留故障记录。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L189-L198】|
| 任意 | 定时巡检发现 `processTime` 早于当前且 `scheduleTime` 超时 | - | WAITING (`0`) | `fetchNonBroadcastEvents`/`updateScheduleTime` 将事件再次纳入执行队列。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L137-L165】|

## 8. 接口契约（I/O 表）
| 接口 | 输入 | 输出 | 关键字段 | 约束/错误 |
| --- | --- | --- | --- | --- |
| `ISequenceGenerator.generateLong` | `seqName`、`useDefault` | Long 序列值 | `seqName`、`cacheSize`、`stepSize` | 未找到序列且 `useDefault=false` 时回退 UUID；数据库更新失败会记录 `nop.lock.sys.save-lock-failed` 日志。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L178-L206】|
| `ICodeRuleGenerator.generate` | 规则名、业务对象 | 编码字符串 | `codePattern`、`seqName` | 缺失规则抛 `ERR_SYS_UNKNOWN_CODE_RULE`，缺失序列名抛 `ERR_SYS_CODE_RULE_EMPTY_SEQ_NAME`。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/coderule/SysCodeRuleGenerator.java†L52-L66】|
| `INamingService.registerInstance` | `ServiceInstance` | void | `instanceId`、`serviceName`、`metadata` | 若记录存在则更新 `updateTime`，否则插入；临时实例需定期续约避免被清理。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/naming/SysDaoNamingService.java†L72-L123】|
| `INamingService.getServices` | - | 服务名列表 | `serviceName`、`updateTime` | 仅返回更新时间在 `clock.getMinCurrentTimeMillis()-getMaxUpdateInterval()` 之后的实例，心跳超时的服务不会出现在结果中。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/naming/SysDaoNamingService.java†L94-L109】|
| `INamingService.getInstances` | 服务名 | `ServiceInstance` 列表 | `updateTime`、`isEphemeral`、`metadata` | 查询时再次校验 `updateTime>System.currentTimeMillis()-getMaxUpdateInterval()`，返回的实例会将 JSON metadata 和标签恢复为 Map/集合；超时实例被过滤。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/naming/SysDaoNamingService.java†L109-L163】|
| `IResourceLockManager.tryLockWithLease` | 资源 ID、持有者、等待/租期 | `IResourceLockState` 或 null | `lockGroup`、`lockName`、`leaseTime` | 等待超时返回 null；成功后必须调用 `releaseLock` 或 `tryResetLease` 释放。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/SysDaoResourceLockManager.java†L69-L233】|
| `IMessageService.sendAsync` | 主题、消息、选项 | `CompletionStage<Void>` | `eventTopic`、`eventData`、`partitionIndex` | 持久化失败返回已完成的异常 `CompletionStage`；消息存储后由轮询线程异步分发。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L265-L312】|
| `IMessageService.subscribe` | 主题、监听器、订阅选项 | `IMessageSubscription` | `topic`、`MessageSubscribeOptions` | 直接委派给 `LocalMessageService.subscribe`，继承内存总线的广播/Ack 处理逻辑，可在数据库事件与本地监听之间复用统一接口。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L303-L313】【F:nop-message/nop-message-core/src/main/java/io/nop/message/core/local/LocalMessageService.java†L87-L200】|

## 9. 用例（Few-shot）
1. **正向**：Given 系统首次启动且 `nop_sys_sequence` 为空 When IoC 初始化触发 `SysSequenceGenerator.lazyInit` Then 自动插入 `seqName=default`、`nextValue` 为配置值的记录，后续业务可直接获取序列。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L138-L175】
2. **反向**：Given 运维误删编码规则的 `seqName` When 调用 `SysCodeRuleGenerator.generate` Then 抛出 `ERR_SYS_CODE_RULE_EMPTY_SEQ_NAME`，提示需补充序列名称或调整规则配置。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/coderule/SysCodeRuleGenerator.java†L62-L66】
3. **边界**：Given 启用了租户隔离但启动阶段尚未设置 `currentTenantId` When 校验系统字典存在性 Then `SysDictLoader.existsDict` 直接返回 true 跳过数据库查询，避免初始化流程因上下文缺失失败。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/dict/SysDictLoader.java†L82-L86】
4. **反向**：Given 锁记录刚创建且租约未到期 When 第二个节点调用 `tryLockWithLease` Then `isExpired` 因 `>=` 判定返回 true，进而触发 `orm().tryDelete(existing)` 删除仍在持有中的锁，需要在修复前通过串行化访问或外部锁避免互斥失效。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/SysDaoResourceLockManager.java†L147-L177】
5. **正向（覆盖最小延迟）**：Given 运维在 `_delta/nop/sys/beans/app-dao.beans.xml` 中为 `nopSysDaoMessageService` 增加 `<property name="minProcessDelay" value="2000"/>` When `updateScheduleTime` 计算重试延迟 Then 结果不会被 10 秒默认值覆盖，而是尊重 2 秒配置，便于快速重试。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L150-L198】【F:nop-sys/nop-sys-dao/src/main/resources/_vfs/nop/sys/beans/app-dao.beans.xml†L31-L36】
6. **Run-less 缺陷复现计划**：Given 两个节点共用数据库锁表且租约为 10s When 按以下时间轴执行 Then 节点 B 会在 100ms 内删除节点 A 尚未过期的行而真正过期的行被保留：
   - Step1：节点 A 在 `t0` 通过 `saveNew` 写入锁行，`expireAt=t0+leaseTime`。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/SysDaoResourceLockManager.java†L179-L198】
   - Step2：节点 B 在 `t0+50ms` 进入 `tryLockWithLease`，捕获插入失败后读取现有锁并调用 `isExpired(existing, clock)`。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/SysDaoResourceLockManager.java†L128-L155】
   - Step3：因为 `isExpired` 判定条件为 `expireAt >= clock.getMaxCurrentTimeMillis()`，节点 B 直接执行 `orm().tryDelete(existing)` 删除记录。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/SysDaoResourceLockManager.java†L147-L177】
   - Step4：节点 B 立即重试 `saveNew` 并成功持有锁；与此同时节点 A 因记录被删丢失互斥，而真正超过 `expireAt` 的旧行在符号反转下不会被删除，需额外巡检清理。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/SysDaoResourceLockManager.java†L162-L172】
7. **Run-less 延迟对照**：Given 调整重试策略返回的 `delay`=1500ms 且默认 `minProcessDelay`=10000ms When 代入 `Math.max(minProcessDelay, delay)` Then 计算结果为 10000ms，推导出的新 `scheduleTime` = `clock.getMaxCurrentTimeMillis()+10000`，证明未覆盖属性时最小延迟仍维持 10 秒；若差量将 `minProcessDelay` 设置为 2000ms，则下一次调度时间下探到 2000ms，可用于在 Excel 表格或脚本中验证覆盖效果。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L150-L198】
8. **反向（重试次数超限）**：Given `SysDaoMessageService` 使用默认 `RetryPolicy` 且某事件已重试两次 When 再次触发异常 Then `getRetryDelay` 返回 `-1`，调度逻辑立即停止并将事件标记为失败或维持原状态，提示需要自定义 `RetryPolicy` 才能允许更多尝试。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L185-L198】【F:nop-commons/src/main/java/io/nop/commons/util/retry/RetryPolicy.java†L17-L180】
9. **正向（扩大广播补偿窗口）**：Given 新节点冷启动且担心错过启动前 5 秒内的广播事件 When 维持默认 `startGap=5000ms` 或在差量中调大该值 Then `doStart` 会设置 `startTime=currentTime-startGap` 并基于该窗口补偿历史广播消息，避免漏消费近期事件。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L88-L134】
10. **反向（乐观锁仅更新部分事件）**：Given 多个节点并发执行 `updateScheduleTime` When `dao.tryUpdateManyWithVersionCheck` 只返回部分成功记录 Then 剩余事件仍以 `eventStatus=WAITING` 保存在数据库并在下一轮 `fetchNonBroadcastEvents` 中再次被抓取，若日志持续出现同一事件需检查版本冲突或调大 `fetchSize`。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L137-L165】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L238-L253】
11. **正向（Ack 主题响应）**：Given 广播或非广播监听器返回 `Acknowledge` When `LocalMessageService.handleMessageResult` 处理返回值 Then 服务会向 `ack-<topic>` 主题发送应答消息，使订阅方可在独立通道接收处理结果。【F:nop-message/nop-message-core/src/main/java/io/nop/message/core/local/LocalMessageService.java†L164-L200】【F:nop-api-core/src/main/java/io/nop/api/core/message/IMessageSubscriber.java†L10-L24】
12. **边界（序列缓存占位延迟生效）**：Given 某业务首次请求一个尚未在数据库配置的序列且 `useDefault=false` When `SysSequenceGenerator.findSeqItem` 创建 `useUuid=true` 的占位项并写入 `defaultCache` Then 在 60 秒 TTL 内再次请求会继续得到随机值；只有在运维补齐序列后调用 `removeCache(seqName)` 或等待 TTL 过期，后续 `generateLong/String` 才会命中新配置返回递增值。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L52-L239】
13. **边界（异步 Ack 完成）**：Given 监听器返回 `CompletionStage<Acknowledge>` When 回调正常完成 Then `LocalMessageService` 在 `whenComplete` 中再次调用 `handleMessageResult`，将应答写入 `ack-<topic>`；若回调抛出异常则只记录 `nop.message.consumer-error` 日志而不会发送应答，需要调用方自行处理失败场景。【F:nop-message/nop-message-core/src/main/java/io/nop/message/core/local/LocalMessageService.java†L152-L200】
14. **Run-less（序列缓存刷新）**：Given `Order` 序列缺失且历史请求已触发 UUID 占位 When 记录 `generateString("Order", false)` 在补齐数据库配置前后的返回值并调用 `removeCache("Order")` Then 可观察到主缓存立即命中新配置，但 fallback 仍需等待 60 秒 TTL 或进程重启才能完全淘汰旧占位，支撑运行手册中的缓存失效流程设计。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L52-L239】
15. ⚠️ **Run-less（雪花 workerId 碰撞评估）**：Given 未显式配置 `nop.sys.seq.snowflake-worker-id` 的多节点集群 When 统计各节点的 `CFG_HOST_ID`、本机 IP 与 `HashHelper.murmur3_32(hostId)%1024` Then 可得出默认 workerId 对照表；再模拟节点重启或容器漂移，观察 hostId 是否稳定以及是否需要显式配置 workerId 以防碰撞。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L104-L135】【F:nop-api-core/src/main/java/io/nop/api/core/config/AppConfig.java†L47-L60】【F:nop-commons/src/main/java/io/nop/commons/crypto/HashHelper.java†L159-L165】
13. **正向（Follower 抢占 Leader）**：Given `checkElection` 在 follower 分支读取到 `leader=null` 或数据库记录已超时 When 调用 `tryBecomeLeader`/`changeLeader` Then `NopSysClusterLeader` 会写入新的 `leaderId`、Epoch+1 和刷新后的租约，随后触发 `onBecomeLeader` 与 `onElectionCompleted` 通知监听器完成角色切换。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/elector/SysDaoLeaderElector.java†L133-L186】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/elector/SysDaoLeaderElector.java†L194-L216】
14. **反向（变更 Leader 失败需重试）**：Given 并发节点同时调用 `changeLeader` When 方法因 `orm_readonly()` 返回 false Then 仅打印 `nop.cluster.leader-elector.change-leader-fail` 日志并在下一轮 `checkElection` 再次尝试，提示需要监控失败重试次数以评估延迟。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/elector/SysDaoLeaderElector.java†L204-L216】
15. **正向（缩短心跳窗口）**：Given 通过差量为 `SysDaoNamingService` 配置 `<property name="autoUpdateInterval" value="PT30S"/>` 并同步设置较短的 `cleanupInterval` When 服务实例每 20 秒续约一次 Then `getMaxUpdateInterval()` 计算出的 31 秒窗口仍能覆盖心跳，`cleanup` 不会误删仍在续约的临时节点。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/naming/SysDaoNamingService.java†L44-L123】
16. **反向（临时实例未续约被清理）**：Given 服务实例以临时模式注册且超过 `autoUpdateInterval` 未刷新 When 定时任务执行 `cleanup` Then 查询条件会删除 `updateTime` 落后 `2*getMaxUpdateInterval()` 的临时记录，导致实例从服务列表消失，提示需要按心跳周期调用 `registerInstance`/`updateInstance` 或改用持久实例。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/naming/SysDaoNamingService.java†L44-L123】
17. **反向（节点时钟漂移导致实例被误判）**：Given 某节点本地时间比数据库慢 15 秒 When `getInstances` 以 `System.currentTimeMillis()-getMaxUpdateInterval()` 构造阈值 Then 即使实例刚通过 `registerInstance` 更新 `updateTime` 也可能被过滤，列表中缺失该节点；需在部署层面同步系统时钟或引入统一时间源。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/naming/SysDaoNamingService.java†L72-L163】
18. **正向（审计日志入库）**：Given 业务实体模型在 `_app.orm.xml` 中标记 `audit`/`audit-save` When 执行保存或更新 Then `OrmEntityChangeLogInterceptor` 会根据脏属性克隆基础日志并写入 `NopSysChangeLog`，记录操作人、业务键及旧值/新值，便于后续追溯。【F:nop-sys/nop-sys-dao/src/main/resources/_vfs/nop/sys/orm/_app.orm.xml†L1-L622】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/log/OrmEntityChangeLogInterceptor.java†L18-L94】
19. **反向（仅配置 audit-save 导致更新缺失）**：Given 模型只添加 `orm:audit-save` 标签 When 执行更新或删除 Then `postUpdate`/`postDelete` 分支返回且不会写入 `NopSysChangeLog`，需补齐 `orm:audit` 标签或在差量中声明，防止审计缺口。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/log/OrmEntityChangeLogInterceptor.java†L28-L83】【F:nop-sys/nop-sys-dao/src/main/resources/_vfs/nop/sys/orm/_app.orm.xml†L1-L622】

20. **反向（关闭审计开关导致全局失效）**：Given 在配置中心设置 `nop.orm.audit.enabled=false` When 业务实体执行保存或更新 Then `nopOrmEntityChangeLogInterceptor` 因 `feature:on` 条件不成立未被注册，`NopSysChangeLog` 不会新增记录，需在生产环境确认开关状态或提供替代日志方案。【F:nop-sys/nop-sys-dao/src/main/resources/_vfs/nop/sys/beans/app-dao.beans.xml†L31-L36】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/log/OrmEntityChangeLogInterceptor.java†L18-L94】
21. **边界（forceUnlock 删除活跃锁）**：Given 运维在锁仍被业务持有时手动调用 `forceUnlock` When `SysDaoResourceLockManager.deleteById` 直接删除 `nop_sys_lock` 行 Then 原持有者的任务失去互斥保护且不会收到错误提示，可能造成重复执行或数据冲突，必须先确认业务已放弃锁再执行兜底删除。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/SysDaoResourceLockManager.java†L90-L99】
22. **正向（MakerChecker 入库审批请求）**：Given `nop.graphql.maker-checker.enabled=true` 且 `IMakerCheckerProvider.isMakerCheckerEnabled` 返回 true When GraphQL 触发带 `@BizMakerChecker` 的 `save`/`update` 操作 Then 执行器会通过 `MakerCheckerTryServiceAction` 调用 try 方法、构造 `SendForCheckRequest` 并写入 `NopSysCheckerRecord`，返回审批请求编号供前端提示等待审批。【F:nop-graphql/nop-graphql-core/src/main/java/io/nop/graphql/core/GraphQLConfigs.java†L40-L42】【F:nop-graphql/nop-graphql-core/src/main/java/io/nop/graphql/core/engine/GraphQLExecutor.java†L205-L224】【F:nop-biz/src/main/java/io/nop/biz/crud/CrudBizModel.java†L504-L520】【F:nop-biz/src/main/java/io/nop/biz/makerchecker/MakerCheckerTryServiceAction.java†L32-L67】【F:nop-sys/nop-sys-dao/src/main/resources/_vfs/nop/sys/orm/_app.orm.xml†L206-L276】
23. **反向（MakerChecker 未启用直接写库）**：Given `nop.graphql.maker-checker.enabled` 保持默认 false 或 Provider 判断不启用 When GraphQL 执行相同的 `save`/`update` 动作 Then `GraphQLExecutor` 直接调用原始 `IDataFetcher` 执行数据库写入，不会生成 `NopSysCheckerRecord`，需要通过配置或 Provider 控制审批策略。【F:nop-graphql/nop-graphql-core/src/main/java/io/nop/graphql/core/GraphQLConfigs.java†L33-L44】【F:nop-graphql/nop-graphql-core/src/main/java/io/nop/graphql/core/engine/GraphQLExecutor.java†L205-L236】
24. **边界（MakerChecker Try 支持 ApiResponse/CompletionStage）**：Given try 方法返回 `ApiResponse` 或 `CompletionStage<ApiResponse>` When `MakerCheckerTryServiceAction` 包装结果 Then `toTryResponse` 会统一填充 `ApiResponse.tryResponse`，并在 `CompletionStage` 完成后通过 `ApiResponse.buildSuccess(reqId)` 返回审批编号，保证同步/异步场景返回结构一致。【F:nop-biz/src/main/java/io/nop/biz/makerchecker/MakerCheckerTryServiceAction.java†L32-L67】
25. **正向（启用命名服务清理定时器）**：Given 在 `_delta/nop/sys/beans/app-dao.beans.xml` 为 `SysDaoNamingService` 增加 `<property name="cleanupInterval" value="…"/>` When 服务启动触发 `@PostConstruct` Then `GlobalExecutors.globalTimer().scheduleWithFixedDelay` 会按照配置周期执行 `cleanup`，将超过 `2*getMaxUpdateInterval()` 的临时实例删除，避免心跳过期节点长期占据注册表。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/naming/SysDaoNamingService.java†L52-L80】
26. **反向（metadata 超长导致注册失败）**：Given 服务实例 metadata 填充超过 1000 字符或标签拼接超出 100 字符 When `registerInstance` 调用 `dao.saveEntity`/`updateEntity` Then 数据库会报列长度超限或静默截断，实例注册失败或标签缺失，需在客户端限制 metadata 体积或拆分标签字段。【F:nop-sys/nop-sys-dao/src/main/resources/_vfs/nop/sys/orm/_app.orm.xml†L553-L589】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/naming/SysDaoNamingService.java†L103-L163】
27. **正向（续约成功刷新版本）**：Given 持有者在租约尚未到期前调用 `tryResetLease(lockState, newLease)` When 数据库执行更新成功 Then 返回值为 true、`NopSysLock.version` 自增且 `expireAt` 以数据库估算时钟为基准前移，调用方应立即刷新 `IResourceLockState` 以反映新版本供后续释放或续约使用。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/SysDaoResourceLockManager.java†L200-L219】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/EntityResourceLockState.java†L7-L44】
28. **反向（续约失败需重新获取）**：Given 另一个节点已抢占并更新了同一锁记录 When 原持有者仍使用旧版本调用 `tryResetLease` Then SQL 返回 0 行导致方法返回 false，锁保持在对方版本；调用方必须重新调用 `tryLockWithLease` 争夺锁或根据业务放弃操作，不能继续依赖旧 `lockState`。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/SysDaoResourceLockManager.java†L200-L219】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/EntityResourceLockState.java†L7-L44】
29. **边界（清理阈值受节点时钟影响）**：Given 数据库时间为基准且节点本地时钟比数据库快 45 秒 When `cleanup` 以 `System.currentTimeMillis()-2*getMaxUpdateInterval()` 计算阈值 Then 刚在数据库刷新心跳的临时实例会被提前删除，因为删除判断使用节点本地时间；若节点时钟滞后同样会让过期实例延迟清理，需在运维层同步时钟或监控清理日志。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/naming/SysDaoNamingService.java†L52-L123】
30. **正向（释放前确认持有者）**：Given 长事务执行时间接近租约 When 在提交前调用 `isHoldingLock(lockState)` Then 该方法会使用新 Session 重新加载锁行并比对 `lockerId` 与 `version`，返回 true 后再调用 `releaseLock` 可确保只删除当前持有的记录。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/SysDaoResourceLockManager.java†L204-L233】
31. **反向（旧版本释放失败）**：Given 另一节点已续约并提升了锁版本 When 原持有者仍携带旧的 `lockState` 调用 `releaseLock` 或继续续约 Then SQL 更新/删除返回 0 行，`isHoldingLock` 也会返回 false，提示锁已易主需重新竞争或放弃操作，避免错误释放。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/SysDaoResourceLockManager.java†L200-L233】
32. **正向（首次抢锁成功）**：Given 数据库中不存在目标资源的锁记录 When 调用 `tryLockWithLease(resourceId, lockerId, waitTime, leaseTime, reason)` Then `saveNew` 在 `runLocal` 事务中插入 `NopSysLock`，立即返回包含 `holderId`、`holderAdder`、`lockReason` 与版本号的 `EntityResourceLockState`，供调用方后续续约或释放。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/SysDaoResourceLockManager.java†L99-L123】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/EntityResourceLockState.java†L7-L44】
33. **反向（等待超时返回 null）**：Given 另一节点始终持有锁且 `waitTime` 小于其租约剩余时间 When `tryLockWithLease` 多次插入失败并在循环中发现 `leftWait<=0` Then 方法记录 `nop.lock.try-lock-fail` 日志并返回 `null`，调用方需视为锁获取失败而不是继续使用未初始化的锁状态。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/SysDaoResourceLockManager.java†L101-L150】
34. **正向（自定义锁持有地址）**：Given 部署环境通过差量为 `SysDaoResourceLockManager` 注入实现了机房内网优先的 `IServerAddrFinder` When `tryLockWithLease` 首次插入锁记录 Then `saveNew` 会将 `AppConfig.appName()` 与 Finder 返回的地址写入 `appId`、`holderAdder`，便于排查多网卡或容器场景下的锁持有节点。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/SysDaoResourceLockManager.java†L42-L67】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/SysDaoResourceLockManager.java†L179-L197】
35. **正向（覆盖默认等待/租期）**：Given 业务未显式传入 `waitTime/leaseTime` 且锁持有时间需要超过 10 秒 When 在 `_delta/nop/sys/beans/app-dao.beans.xml` 为 `SysDaoResourceLockManager` 设置 `<property name="defaultWaitTime" value="5000"/>`、`<property name="defaultLeaseTime" value="60000"/>` Then `getLock()` 创建的 `ResourceLock` 会自动采用新的等待/租期窗口，后续 `tryLockWithLease` 与 `tryResetLease` 也以覆盖值计算剩余时间，避免忘记在调用处统一传参。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/lock/SysDaoResourceLockManager.java†L38-L123】
36. **正向（ApiRequest 保留业务上下文）**：Given 通过 `SysDaoMessageService.sendAsync` 发送 `ApiRequest` 且在 headers 中设置 `bizKey`、`svcName`、`svcAction` When `SysEventHelper.toSysEvent` 序列化消息 Then 数据库事件会携带 `eventHeaders`、`selection`、`eventData`、`bizKey`/`bizObjName` 并基于 `bizObjName|bizKey` 计算稳定的 `partitionIndex`，后续 `fromSysEvent` 可完整还原请求并保持同一业务键的顺序消费。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L266-L304】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysEventHelper.java†L19-L69】
37. **反向（普通对象缺失元数据）**：Given 直接发送非 `ApiRequest` 的普通 Java 对象且未设置 `bizKey` When `SysEventHelper.toSysEvent` 入库事件 Then 只会记录消息类名并使用 `MathHelper.random()` 生成 `partitionIndex`，数据库事件缺乏 headers/selection/data 还原能力，消费顺序也无法按业务键稳定排序，需改用 `ApiRequest` 包装消息以附带元数据。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L266-L304】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysEventHelper.java†L32-L69】
38. **正向（默认序列兜底）**：Given 新业务尚未为 `Order` 定义专用序列且调用 `generateLong("Order", true)` When `findSeqItem` 未命中数据库记录 Then 会回退 `SeqItem("default")` 并返回默认序列的下一个值，保证流程不中断。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L207-L269】
39. **反向（禁用默认导致随机值）**：Given 相同场景但调用 `generateString("Order", false)` When 数据库仍缺少 `Order` 序列 Then `findSeqItem` 会创建 `useUuid=true` 的占位项并缓存，后续生成的字符串均来自随机 UUID，不再保证递增语义，需尽快补齐专用序列。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L207-L269】
40. **边界（缓存占位导致配置延迟生效）**：Given 某序列首次请求时未在数据库配置且 `findSeqItem` 将 `useUuid=true` 的占位项写入 `defaultCache` When 随后补充数据库配置但未调用 `removeCache(seqName)` Then JVM 会在 60 秒 TTL 内继续返回随机值；需在创建真实序列后手动清除缓存或等待 TTL 失效。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L52-L214】
41. **Run-less（序列独立事务验证）**：Given 外层事务中连续调用 `generateLong("Order", true)` 消耗缓存并制造 `syncFromDb` When 外层事务随后回滚 Then 因 `runLocal(syncFromDb)` 在独立 Session 与 `REQUIRES_NEW` 事务里提交了 `nextValue` 前移，回滚后再次调用仍返回递增值；需记录 `next_value` 变更与调试日志以规划跳号补偿策略。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L206-L239】
42. **正向（覆盖默认序列起始值）**：Given 业务希望默认序列 `default` 从 1000 开始编号 When 在配置中心设置 `nop.sys.seq.default-seq-init-next-value=1000` 并触发 `SysSequenceGenerator.lazyInit` Then 插入的默认序列记录会以 1000 作为 `nextValue`，后续回退到 `default` 的业务也从新起点递增，确保线下同步或历史迁移后的编号连续。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L104-L164】
43. ⚠️ **Run-less（默认序列起始值核对）**：Given 预生产环境通过配置修改了 `nop.sys.seq.default-seq-init-next-value` When 在无默认记录的租户执行 `lazyInit` 并导出 `nop_sys_sequence` 表 Then 可以对比 `seq_name='default'` 的 `next_value` 与配置值，若存在历史残留需记录差异并制定补齐或重新初始化方案。为加速 Run-less 记录，可参照下表整理“缺失→清缓存→TTL 过期”的三阶段观测（A：`generateLong(..., false)` 得到 UUID；B：新建序列 + `removeCache`，确认立即返回递增值；C：等待 ≥60s 或重启后再次调用，验证 defaultCache 失效）。需要同步保留 `dao.next_sequence` DEBUG 日志与 `SELECT seq_name,use_uuid,next_value FROM nop_sys_sequence` 的 SQL 快照，便于后续更新证据矩阵。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L52-L214】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L104-L164】
44. **反向（步长配置为 0 被重置）**：Given DBA 误把 `NopSysSequence.step_size` 更新为 0 When `SysSequenceGenerator` 下一次通过 `syncFromDb` 装载该序列 Then `SeqItem.update` 会自动把步长改回 1 并按照 `cacheSize*1` 计算批量窗口，日志不会提示配置错误但会导致跳号小于预期，应在 SQL 维护脚本中避免写入非正值。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L62-L206】
45. ⚠️ **Run-less（步长归一影响评估）**：Given 计划把序列批量扩大为 `cacheSize=500`、`stepSize=0` 以观测归一行为 When 依次导出缓存耗尽前后的 `nop_sys_sequence.next_value` 与 `SysSequenceGenerator` 调试日志 Then 可确认数据库写回依旧按步长 1 递增，帮助评估是否需要修正维护脚本并在上线前补充批量配置检查表。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L62-L206】
46. ⚠️ **Run-less（消息线程回退验证）**：Given 未在差量中显式注入 `executor`/`timer` 并希望确认默认线程来源 When 启动 `SysDaoMessageService`、开启 `nop.sys.dao.message.log-level=DEBUG` 并保留 `doStart` 日志 Then 可在输出中看到 `GlobalExecutors.globalWorker()`/`globalTimer()` 被采用；若随后覆盖其中一个属性需同步配置另一个并截取覆盖前后日志与线程名，确认未遗漏定时器或导致轮询停滞。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L63-L134】【F:nop-commons/src/main/java/io/nop/commons/async/GlobalExecutors.java†L25-L96】
47. IF 通过 `SysDaoMessageService.sendMultiAsync` 批量写入事件 THEN 方法会在一次数据库时钟快照内构造所有 `NopSysEvent` 并让 `eventTime/processTime` 共享同一时间戳，非 `ApiRequest` 负载仍使用随机分区；若需要逐条时间戳或按业务键顺序消费，应改用逐条 `sendAsync` 或确保消息包装为 `ApiRequest`。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L266-L304】
48. ⚠️ **Run-less（雪花序列同毫秒采样）**：Given 多线程或批处理在同一毫秒内高频调用 `generateLong` When 打开 `dao.next_sequence` 调试日志并采集同毫秒生成的 ID Then 可观察到序列在同毫秒溢出时随机重置为 `0-99` 且必要时等待到下一毫秒，新的毫秒周期也会随机选择起始序列；需整理样本与日志评估热点与随机分布，并记录时钟回拨触发的自旋等待。【F:nop-dao/src/main/java/io/nop/dao/seq/SnowflakeSequenceGeneator.java†L76-L129】【F:nop-commons/src/main/java/io/nop/commons/util/MathHelper.java†L193-L201】
49. IF 需要让消息重试在固定时间间隔内执行而避免 30% 抖动和指数回退 THEN 应在差量 Bean 或配置中心注入自定义 `RetryPolicy`，显式调用 `setExponentialDelay(false)` 与 `setJitterRatio(0)` 或设定确定性的 `retryDelay/maxRetryDelay`，否则默认策略会按 1 秒基准指数增长并随机化延迟，导致实际重试时间分布在基准值上下浮动。【F:nop-commons/src/main/java/io/nop/commons/util/retry/RetryPolicy.java†L17-L121】【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L60-L99】
## 10. 证据矩阵
| 结论 | 证据 | 置信度 | Run-less 验证计划 |
| --- | --- | --- | --- |
| `nop-sys-dao` 聚合系统级实体与字典/事件 schema | `_app.orm.xml` 中的实体与字典定义 | 高 | 对照 `_gen` 生成的实体类或数据库脚本，确认字段一致性。 |
| 序列生成器支持默认初始化、Snowflake、UUID 三路线 | `SysSequenceGenerator` 源码 | 高 | 检查实际部署配置或日志，确认默认序列是否被插入及 workerId 配置来源。 |
| 序列默认缓存 `defaultCache` 仅能等待 60 秒 TTL 或重启失效 | `SysSequenceGenerator` 创建 `defaultCache` 及 `clearCache/removeCache` 实现仅操作主缓存 | 中 | 记录缺失序列触发 UUID 占位→调用 `removeCache`→等待 60 秒 TTL 的返回值差异，佐证 fallback 缓存仍需自然过期。 |
| 数据库消息服务依赖定时轮询与重试策略 | `SysDaoMessageService` 调度/重试实现 | 中 | 收集一次 `nop_sys_event` 处理日志，验证 `retryTimes` 与状态转移。 |
| `SysDaoResourceLockManager` 过期判定符号反转 | `isExpired` 使用 `expireAt >= clock.getMaxCurrentTimeMillis()`，与租约写入逻辑组合后允许删除未过期锁、保留已过期锁 | 高 | 依照 Run-less 时间轴（Step1–Step4）撰写缺陷描述并补充串行化/外部互斥规避方案，同时跟踪官方修复。 |
| 锁续约会自增版本并依赖数据库估算时钟 | `tryResetLease` 使用 `dao.getDbEstimatedClock()` 计算新的 `expireAt`，`SQL` 设置 `version+1` 并仅在返回 1 行时续约成功 | 中 | 记录一次成功/失败的 `tryResetLease` 调用日志，确认续约后需刷新 `IResourceLockState`，失败则重新竞争锁。 |
| 锁持有校验与释放依赖版本一致性 | `isHoldingLock` 通过新 Session 重读行并比较 `lockerId`/`version`，`releaseLock` 直接删除实体，若版本落后则删除失败 | 中 | 捕获一次 `isHoldingLock` 成功/失败与 `releaseLock` 的日志，验证刷新本地 `lockState` 后释放才不会误删他人锁。 |
| `tryLockWithLease` 等待超时会返回 `null` 并打印失败日志 | 循环计算 `leftWait`，若等待耗尽则记录 `nop.lock.try-lock-fail` 并直接返回 `null`，未创建锁状态 | 中 | 设计 Run-less 时间轴模拟等待超时，记录 `nop.lock.try-lock-fail` 日志与返回值，确认调用方按失败路径处理。 |
| `forceUnlock` 直接删除锁记录且不校验版本/持有者 | `forceUnlock` 通过 `orm().deleteById` 删除 `nop_sys_lock` 行，不比对版本或 `lockerId` | 中 | 在演练环境人工调用 `forceUnlock` 前后对比 `nop_sys_lock` 行与业务日志，评估兜底操作是否会打断仍在执行的任务。 |
| 锁记录会写入应用名与持有者地址，便于追踪节点 | `saveNew` 使用 `AppConfig.appName()` 与 `IServerAddrFinder`/`NetHelper.findLocalIp()` 填充 `appId`、`holderAdder` 字段 | 中 | 收集一次自定义 `IServerAddrFinder` 或容器场景下的锁记录，确认数据库中的 `app_id`、`holder_adder` 与预期地址一致。 |
| 缺失序列会被 `defaultCache` 缓存 60 秒并继续返回随机值 | `defaultCache` 以 500 条、60 秒 TTL 缓存占位项，`removeCache`/`clearCache` 可手动失效缓存，补齐数据库配置前的占位仍会 `useUuid=true` | 中 | 在缺少序列时调用 `generate*` 获取随机值，随后创建真实序列并调用 `removeCache`，比对新旧返回值与日志，确认缓存清理后能立即生效。 |
| 雪花序列 workerId 默认由 hostId/IP 哈希 1024 得出 | `afterPropertiesSet` 在 workerId 为 0 时读取 `CFG_HOST_ID`，空值回退本机 IP，再通过 `HashHelper.murmur3_32` 取模 1024 生成 workerId。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/seq/SysSequenceGenerator.java†L126-L135】【F:nop-api-core/src/main/java/io/nop/api/core/ApiConfigs.java†L25-L36】【F:nop-commons/src/main/java/io/nop/commons/crypto/HashHelper.java†L159-L165】 | 中 | 收集实际部署的 `CFG_HOST_ID`/IP 与启动日志，核对默认 workerId 是否稳定一致，并评估多节点哈希碰撞风险或是否需要显式配置。 |
| 雪花序列在同毫秒内随机初始化序列号并在溢出时自旋到下一毫秒 | `SnowflakeSequenceGeneator.generateLong` 在新毫秒或序列溢出时使用 `MathHelper.secureRandom().nextInt(100)` 初始化 0–99 的序列值，并在溢出后调用 `tilNextMillis` 等待时间推进；`MathHelper.secureRandom()` 默认返回单例 `DefaultSecureRandom` 且可通过 `registerSecureRandomImpl` 注入测试实现。【F:nop-dao/src/main/java/io/nop/dao/seq/SnowflakeSequenceGeneator.java†L76-L129】【F:nop-commons/src/main/java/io/nop/commons/util/MathHelper.java†L193-L201】 | 中 | 在同毫秒内批量采集 Snowflake ID 与 `dao.next_sequence` 调试日志，确认随机起始、自旋等待与可插拔随机器的影响，并评估热点与冲突风险。 |
| 默认序列起始值可通过 `nop.sys.seq.default-seq-init-next-value` 配置 | `SysSequenceGenerator.lazyInit` 在序列表为空时按配置插入默认记录，`setDefaultSeqInitNextValue` 读取 `@cfg:nop.sys.seq.default-seq-init-next-value` 注入起始值 | 中 | 在新环境执行 `lazyInit` 前后导出 `nop_sys_sequence` 表或调试日志，确认 `seq_name='default'` 的 `next_value` 与配置一致，并在需要重置时结合清缓存与更新脚本验证效果。 |
| 默认序列插入时携带 `cacheSize=100`、`stepSize=1` | `addDefaultSequence` 通过 `runLocal` 保存默认记录时固定这两个字段并在并发时捕获重复键异常；`syncFromDb` 会沿用 `cacheSize` 批量更新 `nextValue` | 中 | 查看 `nop_sys_sequence` 表的默认记录或日志，确认 `cache_size/step_size` 是否仍为 100/1，并在修改后结合 `removeCache` 与等待 `defaultCache` TTL 的步骤验证新的批量设置是否生效。 |
| 序列批量写回遵循 `cacheSize*stepSize`，非正步长会被自动归一到 1 | `SeqItem.update` 在装载时将 `stepSize<=0` 重置为 1，`syncFromDb` 依据 `cacheSize` 与步长计算新的 `nextValue` 批量窗口并更新数据库 | 中 | 记录 `cacheSize`、`stepSize` 配置与 `syncFromDb` 写回的 `next_value` 变化，验证步长归一逻辑与批量跳号范围，必要时结合 SQL/日志评估大批量配置的风险。 |
| 序列同步在独立会话与新事务中执行，避免外层缓存污染 | `runLocal` 通过 `ormTemplate.runInNewSession` + `transactionTemplate.runInTransaction(..., REQUIRES_NEW, …)` 执行 `syncFromDb`，刷新 `nextValue` 前会锁定 `NopSysSequence` 并在独立事务内提交 | 中 | 收集一次序列耗尽触发 `syncFromDb` 的调试日志，比较外层事务提交与回滚两种情况下的 `next_value` 变化，确认独立事务已生效并补充业务跳号补偿建议。 |
| `defaultWaitTime/defaultLeaseTime` 支撑 `ResourceLock` 默认窗口 | `getDefaultWaitTime`/`getDefaultLeaseTime` 返回 Bean 属性值，`getLock`/`ResourceLock` 会在未传入参数时使用这些默认值 | 中 | 在差量 Bean 覆盖等待/租期并通过日志或锁表快照确认新的默认窗口已生效，必要时与调用处显式传参对照。 |
| 服务实例心跳与清理取决于 `autoUpdateInterval`/`cleanupInterval` | `SysDaoNamingService` 以 `getMaxUpdateInterval()` 计算心跳上限（默认 60s 或自定义值 +1s），并在 `cleanup` 中删除超过 `2*getMaxUpdateInterval()` 的临时实例 | 中 | 收集一次注册/续约与 `cleanup` 的日志或数据库快照，验证自定义心跳配置与临时实例清理效果。 |
| `cleanup` 使用本地系统时间推导删除阈值，漂移会改变清理窗口 | `cleanup` 直接调用 `System.currentTimeMillis()` 计算 `updateTime` 截止值，而 `registerInstance`/`getServices` 使用数据库估算时钟，节点时间超前会提前删除仍在续约窗口内的实例，滞后则延迟清理 | 中 | 对比数据库估算时钟与节点系统时间，记录漂移情况下的 `cleanup` 日志，确认是否提前或延后删除临时实例并更新规避策略。 |
| `getInstances` 使用本地时钟过滤实例，存在漂移风险 | `getInstances` 使用 `System.currentTimeMillis()-getMaxUpdateInterval()` 判定有效实例，若节点时钟滞后或超前会导致过期判断失真 | 中 | 比较节点系统时间与数据库估算时钟，模拟时钟漂移下 `getInstances` 的过滤结果并记录缺失或延迟剔除的实例。 |
| 缺失序列会回退默认或随机 UUID，占位结果被缓存 | `findSeqItem` 先查缓存，再尝试 `defaultCache`，缺失序列+`useDefault=true` 时回退 `default`，否则创建 `useUuid=true` 的占位项并缓存 | 中 | 记录 `generateLong/String` 在缺失序列且 `useDefault=true/false` 两种场景的返回值与日志，验证默认序列兜底与随机 UUID 占位的差异。 |
| 自定义心跳窗口会自动增加 1 秒安全裕度 | `getMaxUpdateInterval()` 在传入 `autoUpdateInterval` 基础上加 1000ms，`getServices`/`getInstances` 与 `cleanup` 均使用该阈值过滤节点 | 中 | 调整 `autoUpdateInterval` 并记录心跳与 `cleanup` 日志，确认续约周期必须小于配置值+1s 才能保留实例。 |
| metadata/标签列长度限制注册数据规模 | `NopSysServiceInstance` 的 `META_DATA` 字段限制 1000 字符、`TAGS_TEXT` 限制 100 字符，`SysDaoNamingService` 在注册/查询时直接序列化这些字段 | 中 | 检查注册实例 metadata/标签长度并模拟超限输入，观察数据库是否抛异常或截断，并据此制定客户端校验。 |
| Metadata/标签在注册与查询间保持一致 | `copyToEntity` 把 `ServiceInstance` 的 metadata 序列化为 JSON 与标签字符串，`fromEntity` 反序列化回 Map/集合，保证数据库记录与运行实例字段同步 | 中 | 通过注册实例后查询 `NopSysServiceInstance` 行并调用 `getInstances`，核对 metadata/标签值是否原样恢复。 |
| `SysDaoMessageService` 最小处理延迟需手工覆盖 | 默认 Bean 未设置 `minProcessDelay`，`updateScheduleTime` 使用 `Math.max(minProcessDelay, delay)` 覆盖更小值 | 高 | 依据“Run-less 延迟对照”步骤计算默认/自定义延迟下的 `scheduleTime`，并结合差量 `<property name="minProcessDelay">` 或配置日志确认计算结果生效。 |
| `SysEventHelper` 会在事件入库与读取时保留 `ApiRequest` 元数据并为非请求消息生成随机分区 | `SysEventHelper.toSysEvent/fromSysEvent` 拷贝 headers、selection、data、bizKey/bizObjName，并基于 `bizObjName|bizKey` 计算 `partitionIndex`；`SysDaoMessageService.sendAsync` 在非 `ApiRequest` 场景回退随机分区 | 中 | 收集一条携带 `ApiRequest` 的事件行与一条普通对象事件，导出 `nop_sys_event` 表和处理日志，对比 `event_headers`、`event_data`、`partition_index`，验证顺序消费与随机分区差异，并补齐元数据落库证据。 |
| 批量发送会共用同一数据库时间戳且回退随机分区 | `SysDaoMessageService.sendMultiAsync` 缓存一次 `currentTime` 并为非 `ApiRequest` 消息生成随机 `partitionIndex` | 中 | 记录 `sendMultiAsync` 批量写入前后的 `event_time/process_time` 与 `partition_index`，对比逐条发送或携带 `ApiRequest` 时的变化，补齐批量时间戳共享的证据。 |【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L266-L304】|
| 默认 `RetryPolicy` 仅允许 2 次重试、基准延迟 1s 并附带 30% 抖动/指数退避 | `SysDaoMessageService` 默认持有 `RetryPolicy`；`RetryPolicy` 的 `maxRetryCount=2`、`retryDelay=1000`、`maxRetryDelay=300000`、`exponentialDelay=true`、`jitterRatio=0.3`，当达到重试上限或异常不可恢复时返回 `-1` 终止调度 | 中 | 搜集自定义 `RetryPolicy` 覆盖示例或日志，验证默认指数退避+抖动与 2 次上限的实际效果，并记录需要关闭抖动或增加重试次数的差量配置。 |
| 消息轮询默认回退全局执行器并使用固定窗口参数 | `SysDaoMessageService` 在 `doStart` 中检查 `executor`/`timer` 并回退到 `GlobalExecutors.globalWorker()/globalTimer()`，保留 `checkInterval=500ms`、`fetchSize=100`、`startGap=5000ms` 的缺省窗口。【F:nop-sys/nop-sys-dao/src/main/java/io/nop/sys/dao/message/SysDaoMessageService.java†L63-L134】【F:nop-commons/src/main/java/io/nop/commons/async/GlobalExecutors.java†L25-L96】 | 中 | 记录一份 `doStart` 日志或配置样例，确认覆盖属性后轮询节奏与广播补偿窗口的变化，同时捕获默认线程名以佐证 Rule 47。 |
| 广播/非广播查询依赖 `startGap`、`lastBroadcastEvent` 与 WAITING 状态过滤 | `fetchBroadcastEvents` 要求 `eventTime>startTime` 且使用 `findNext(lastBroadcastEvent, …)`，`fetchNonBroadcastEvents` 仅拉取 WAITING 状态并按 `processTime` 限流 | 中 | 记录 `startGap`、`lastBroadcastEvent` 重置日志或差量配置，并追踪乐观锁未命中的事件在下一轮重新被抓取的行为。 |
| 数据库事件通过 `LocalMessageService` 桥接到订阅与 `ack-` 主题 | `processBroadcastEvent` → `LocalMessageService.invokeMessageListener` 在同步或 `CompletionStage` 完成后调用 `handleMessageResult`，并以 `getAckTopic` 写入仅限本地内存的 `ack-` 主题 | 中 | 收集监听器返回 `Acknowledge`（含异步完成）的日志或订阅样例，确认业务主题与 `ack-` 主题配对策略及失败时的 `nop.message.consumer-error` 记录。 |
| MakerChecker 审批依赖 GraphQL 开关、`@BizMakerChecker` 注解与 Provider 实现 | `GraphQLConfigs` 默认 `nop.graphql.maker-checker.enabled=false`，`GraphQLWebService` 根据开关注入上下文，`ObjectDefinitionExtProcessor` 装配 `MakerCheckerTryServiceAction` 并调用 `IMakerCheckerProvider.sendForCheckAsync`，请求落库于 `NopSysCheckerRecord`/`NopSysMakerCheckerRecord` | 中 | 收集一次启用 MakerChecker 的 GraphQL 调用日志或伪执行脚本，确认返回审批编号与 `NopSysCheckerRecord` 入库字段，若未启用需记录配置或 Provider 判定逻辑。 |
| `MakerCheckerTryServiceAction` 统一封装同步/异步 try 结果并返回审批编号 | `MakerCheckerTryServiceAction` 在 `CompletionStage.thenCompose` 中等待结果，调用 `toApiRequest`/`toTryResponse` 填充请求数据，最终以 `ApiResponse.buildSuccess(reqId)` 返回审批编号 | 中 | 捕获一次带 `@BizMakerChecker` 的 GraphQL 请求，观察返回结构及 `SendForCheckRequest` 中的 `tryResult` 字段，确认同步/异步 try 方法均返回审批编号。 |
| `OrmEntityChangeLogInterceptor` 记录实体变更并自动过滤版本/禁审计列 | `postSave`/`postUpdate`/`postDelete` 根据 `audit`/`audit-save` 标签写入 `NopSysChangeLog`，携带操作人、业务键、审批人、旧值/新值，排除版本列与 `no-audit` 标签，且受 `nop.orm.audit.enabled` 开关控制 | 中 | 捕获一次带 `audit` 模型的保存/更新日志并导出 `NopSysChangeLog` 行，确认旧值/新值、`appId`、`operatorId` 写入情况，并验证关闭开关或缺少 `audit` 标签时的缺口。 |
| 异步 Ack 失败仅留下错误日志且无应答 | `CompletionStage.whenComplete` 中异常分支只记录 `nop.message.consumer-error`，未调用 `handleMessageResult` 写入 `ack-` 主题 | 中 | 触发异步监听器抛出异常，截取 `nop.message.consumer-error` 日志并确认 `ack-` 主题无新消息，用于补齐失败链路证据。 |
| 选主流程依赖 Epoch 校验与租约续期，并可通过 `restartElection` 强制触发重选 | `checkElection` 循环 20 次校验角色，`refreshLeader`/`tryRenewLease` 在安全间隔内刷新 `expireAt`，`changeLeader`/`tryBecomeLeader` 提升 Epoch 并写回 leader 记录，`restartElection` 最多 10 次将 Epoch+1 并把 `expireAt` 置当前时间 | 中 | 收集一次 leader 续约与重选日志，确认 Epoch 提升、租约刷新与 `onElectionCompleted` 的触发顺序。 |

## 11. 更新记录
| 版本 | 日期 | 说明 |
| --- | --- | --- |
| v0.47 | 2024-06-17 | 扩展默认 `RetryPolicy` 的指数退避与 30% 抖动细节，新增固定重试间隔的规则与证据矩阵更新，提醒覆盖 `RetryPolicy` 时需同步配置 `retryDelay/maxRetryDelay` 与抖动参数。 |
| v0.46 | 2024-06-17 | 记录 `sendAsync`/`sendMultiAsync` 共用数据库估算时钟写入时间戳并在批量场景共享 `currentTime`，新增批量发送共用时间戳的事实、规则与证据矩阵条目，提示需要区分时间戳或顺序时改用逐条发送或包装 `ApiRequest`。 |
| v0.45 | 2024-06-17 | 新增“默认序列三阶段采集模板”，细化缺失→清缓存→TTL 过期的 Run-less 步骤并提醒保留 SQL/DEBUG 日志以回填证据矩阵。 |
| v0.44 | 2024-06-17 | 补充消息调度默认回退 `GlobalExecutors` 与订阅桥接说明，新增规则提醒覆盖 `timer/executor` 时同步配置线程池，并在接口契约列出 `IMessageService.subscribe` 以指向本地订阅共用逻辑。 |
| v0.43 | 2024-06-17 | 补充步长归一的反向用例与 Run-less 计划，提醒维护脚本写入 `step_size=0` 会被重置为 1，需结合缓存刷新与批量评估，证据矩阵同步要求记录归一日志。 |
| v0.42 | 2024-06-17 | 补充 `syncFromDb` 会按照 `cacheSize*stepSize` 计算批量窗口并把非正步长归一为 1，新增对应事实、规则与证据矩阵条目，提示修改步长或批量时需评估跳号范围并结合缓存失效策略。 |
| v0.41 | 2024-06-17 | 标注 `addDefaultSequence` 插入默认记录时固定 `cacheSize=100`、`stepSize=1` 并在并发场景捕获重复键，新增相关事实、规则与证据项，提醒调整批量后需配合清缓存与等待 TTL。 |
| v0.40 | 2024-06-17 | 说明 `nop.sys.seq.default-seq-init-next-value` 决定默认序列起始值，新增覆盖默认编号的规则与 Run-less 核对计划，并在证据矩阵加入配置验证条目。 |
| v0.39 | 2024-06-17 | 记录 `MathHelper.secureRandom()` 懒加载 `DefaultSecureRandom` 的事实与可替换接口，提醒雪花序列随机起始依赖安全随机器；证据矩阵同步补充可注入自定义随机源的取证计划。 |
| v0.38 | 2024-06-17 | 记录雪花序列默认 workerId 基于 `CFG_HOST_ID`/本机 IP 哈希取模 1024 的逻辑，并新增确保 hostId 稳定与采集默认取值的行动指引；证据矩阵同步补充 workerId 验证计划。 |
| v0.37 | 2024-06-17 | 补充 `runLocal` 独立事务回滚不回退序列的 Run-less 用例与规则，并提示记录外层事务回滚前后的 `next_value` 以规划补偿；同步强调序列缓存占位需要等待 TTL 失效。 |
| v0.36 | 2024-06-17 | 明确 `removeCache` 仅清理主缓存、`defaultCache` 无公开清除接口，新增事实/规则以提醒需要等待 60 秒 TTL 或重启才能驱逐占位项；证据矩阵与路线图同步要求记录缓存失效窗口。 |
| v0.35 | 2024-06-17 | 强调 `runLocal` 以新 Session/REQUIRES_NEW 事务刷新序列并新增“缓存占位延迟生效”用例、清缓存规则，提示修改数据库序列后需清理 JVM 缓存并关注独立事务前移的影响；证据矩阵同步加入独立事务与缓存清理取证计划。 |
| v0.34 | 2024-06-17 | 补充 `defaultCache` 60 秒 TTL、`clearCache`/`removeCache` 缓存失效接口与“缓存占位导致配置延迟生效”边界用例，提醒在补齐数据库序列后需手动清理缓存；证据矩阵新增默认缓存验证计划。 |
| v0.33 | 2024-06-17 | 扩展序列生成章节，记录 `SeqItem` 缓存批量递增、`syncFromDb` 锁定 `NopSysSequence` 的事务流程，以及缺失序列在 `useDefault=true/false` 下分别回退默认或随机 UUID，占位结果被缓存；新增对应事实、规则、用例与证据矩阵条目。 |
| v0.32 | 2024-06-17 | 补充 `SysEventHelper`/`SysDaoMessageService` 的事件序列化细节，说明 `ApiRequest` 会保留 headers/selection/data 并计算稳定分区，普通对象则回退随机分区，新增对应事实、规则、用例与证据矩阵条目。 |
| v0.31 | 2024-06-17 | 标注 `ResourceLock` 的 `defaultWaitTime/defaultLeaseTime` 覆盖路径与 `getLock` 默认窗口，新增相关事实、用例与证据矩阵条目，提醒在长事务或串行化场景调整 Bean 属性。 |
| v0.30 | 2024-06-17 | 标注锁记录写入 `appId`/`holderAdder` 的来源及 `IServerAddrFinder` 配置路径，新增相关事实、规则、用例与证据矩阵条目，提醒在多网卡或容器环境注入稳定地址并调整默认等待/租期。 |
| v0.29 | 2024-06-17 | 补充 `forceUnlock` 删除流程的事实、规则、用例与证据矩阵条目，提醒该操作不会校验版本/持有者，仅适合作为人工兜底手段。 |
| v0.28 | 2024-06-17 | 增加 `ResourceLock` 默认等待/租期与 `tryLockWithLease` 超时返回 `null` 的事实、规则、用例和证据矩阵，提示需要监控等待耗尽日志并按失败路径处理。 |
| v0.27 | 2024-06-17 | 补充 `isHoldingLock` 校验与 `releaseLock` 删除流程，新增规则、用例与证据矩阵条目，提醒释放前需刷新锁状态并核对版本避免误删他人锁。 |
| v0.26 | 2024-06-17 | 补充锁续约需调用 `tryResetLease` 并刷新版本的事实、规则、用例与证据矩阵条目，提醒续约失败需要重新竞争锁且依赖数据库估算时钟。 |
| v0.25 | 2024-06-17 | 说明 `cleanup()` 仍使用本地 `System.currentTimeMillis()` 推导删除阈值、与数据库时钟混合导致漂移风险，新增相关规则、边界用例与证据矩阵条目，并更新路线图提醒监控清理窗口。 |
| v0.24 | 2024-06-17 | 标注服务实例 metadata/标签的列长度限制与默认 `ephemeral=true` 行为，新增长度风险规则、反向用例与证据矩阵条目，并提示在客户端控制注册数据规模。 |
| v0.23 | 2024-06-17 | 说明 `getInstances` 仍依赖本地 `System.currentTimeMillis()` 过滤实例、存在时钟漂移风险，新增相关 TL;DR、规则、用例与证据矩阵条目。 |
| v0.22 | 2024-06-17 | 记录命名服务使用数据库估算时钟刷新 `updateTime` 及过滤阈值，补充跨节点时间漂移对心跳窗口的影响并新增对应事实/规则。 |
| v0.21 | 2024-06-17 | 标注 `autoUpdateInterval` 会额外增加 1 秒安全裕度，新增心跳窗口配置规则、用例与证据矩阵条目，提醒调整续约周期与清理任务。 |
| v0.20 | 2024-06-17 | 补充服务实例字段与 metadata 序列化、命名服务接口契约、`cleanupInterval` 配置与规则/用例/证据矩阵，提醒需显式开启定时清理以删除过期临时实例。 |
| v0.19 | 2024-06-17 | 记录 MakerChecker 默认关闭、`MakerCheckerTryServiceAction` 封装 `ApiResponse.buildSuccess` 与同步/异步 try 统一行为，补充事实、规则、流程、用例与证据矩阵并更新路线图提醒。 |
| v0.18 | 2024-06-17 | 补充 MakerChecker 审批链条的事实、规则、流程与用例，记录 GraphQL 开关、`@BizMakerChecker` 注解与审批表之间的协作关系，并在路线图/证据矩阵标注新增验证任务。 |
| v0.17 | 2024-06-17 | 强调 `nop.orm.audit.enabled` 开关、`orm:no-audit` 排除策略与关闭审计开关的反向用例，补充流程、用例与路线图调试提示。 |
| v0.16 | 2024-06-17 | 扩展审计日志章节，记录 `nop.orm.audit.enabled` 环境开关、`audit`/`audit-save` 标签差异与 `NopSysChangeLog` 字段定义，新增相关规则、用例与证据矩阵更新。 |
| v0.15 | 2024-06-17 | 补充 `OrmEntityChangeLogInterceptor` 的工作机制，新增审计日志事实、规则、流程、用例与证据矩阵条目，提示需关注模型标签与 `NopSysChangeLog` 入库行为。 |
| v0.14 | 2024-06-17 | 细化 `SysDaoNamingService` 的心跳上限、定时清理与临时实例删除规则，新增相关事实、规则、用例与证据矩阵条目。 |
| v0.13 | 2024-06-17 | 标注 `restartElection` 通过新会话读取最新 leader 行并加入保留 `runInNewSession` 的规则，提醒强制重选需避免缓存污染。 |
| v0.12 | 2024-06-17 | 补充 `SysDaoLeaderElector` 的循环上限、租约续期、Epoch 提升与 `restartElection` 重选机制，新增选主规则、用例与证据矩阵条目。 |
| v0.11 | 2024-06-17 | 明确异步 Ack 失败仅记录 `nop.message.consumer-error` 且不会写入 `ack-` 主题，新增对应规则与 Run-less 验证计划。 |
| v0.10 | 2024-06-17 | 说明 `LocalMessageService` 处理 `CompletionStage` 的应答流程与 `ack-` 主题仅在本地分发的边界，新增异步 Ack 规则、用例与证据矩阵条目。 |
| v0.9 | 2024-06-17 | 补充 `LocalMessageService` 的广播分类与 `ack-` 主题桥接机制，新增应答规则、流程、用例与证据矩阵条目。 |
| v0.8 | 2024-06-17 | 说明广播/非广播查询窗口、`lastBroadcastEvent` 指针与乐观锁回流行为，新增相关规则、反向用例与证据矩阵条目。 |
| v0.7 | 2024-06-17 | 说明消息轮询默认回退全局执行器、500ms 检查间隔与 5s 广播补偿窗口，并新增相关规则、用例与证据矩阵条目。 |
| v0.6 | 2024-06-17 | 补充默认 `RetryPolicy` 的重试上限、规则、反向用例与证据矩阵条目，并提醒需自定义策略才能突破 2 次重试。 |
| v0.5 | 2024-06-17 | 新增 Run-less 消息延迟校准流程与对照用例，指引在未覆盖 `minProcessDelay` 时计算调度时间并更新证据矩阵计划。 |
| v0.4 | 2024-06-17 | 明确 `minProcessDelay` 默认 10 秒及差量覆盖方式，新增规则、用例与证据矩阵条目。 |
| v0.3 | 2024-06-16 | 形成 `SysDaoResourceLockManager.isExpired` 的 Run-less 复现时间轴，完善证据矩阵与后续规避建议。 |
| v0.2 | 2024-06-15 | 明确 `isExpired` 判定反转导致锁被误删的缺陷，补充 `minProcessDelay` 覆盖需求与新增反向用例/证据矩阵条目。 |
| v0.1 | 2024-06-14 | 首次整理 `nop-sys-dao` 的实体基线、核心 Bean、锁/消息/选主流程与潜在风险。 |

## 12. 检索标签（Tags）
`nop-sys-dao` · 序列 · 编码规则 · 系统字典 · 数据库锁 · 选主 · 消息队列 · 注册中心
